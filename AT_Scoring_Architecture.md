# AT Development Journal — Scoring System Architecture

**Status:** Building — sessions 1–4 done; session 5 deployed, **one done-when item open: Chris scores a session blind in the new app (away ~1 week)**; session 6: exemplars live and cited (6a: 6/12 exact · 12/12 within one); 6b failed on the blind session (Evaluate +2), 6c (`v2-rag-4` + evidence guards) live-checked — anchors and Equipment fixed, **Evaluate still +2 on 7uk7lnh pending Chris's two Evaluate chunks; template frozen until the third scorecard** (37 Chris statements from the 9/18 call still pending his approval); session 7 (Journal tab) built and verified offline — **done-when open: needs an `entryType` header added to the Journal sheet and Chris's real address in `_MENTOR_PROFILES` (§17), then six entries, Chris's depth read, the email**; next is 8
**Owner:** Mark Anderson
**Ground truth:** Chris (AT Assessor)
**Purpose of this doc:** Every build session reads this first. It holds every decision and its reason so nothing gets re-derived or re-argued.

---

## 1. Goal

An AI scorer for PSIA Alpine Trainer Movement Analysis sessions accurate enough that Chris and Mark trust it without validating every score. Measured as per-criterion agreement rate between AI scores and Chris's blind scores. Target: ≥85% within-one, ≥60% exact, sustained across a season.

Secondary: stop rebuilding. Adding knowledge (docs, Chris's reasoning, scored examples) must never require touching a prompt or hitting a context limit.

## 2. What exists today (kept)

- **React app** `ATDevelopmentJournal.jsx` (~5,000 lines, single file). 12 tabs. Users: mark (candidate), chris/gates/mike (mentors).
- **Vercel serverless:** `/api/claude` → Anthropic API; `/api/sheet` → Google Apps Script → Google Sheet.
- **Google Sheet, 3 tabs:** Journal, Config, MASessions. Per-row storage for sessions, checkpoints, videos, clinics. Config holds `_THEMES`, `_COACH_NOTES`, `_MENTOR_ASSESSMENTS`, `_REFERENCE_MATERIALS`, `_MENTOR_PROFILES`.
- **Six criteria** scored 1–6 (Fitts & Posner; 4 = pass): Describe, Cause/Effect, Evaluate, Prescription, Bio/Physics, Communication.
- **MA session types:** AT Exam (two-audience: peer + examiner, structured sections), Written MA, Scenario, Reverse MA, Compare & Contrast, Video Analysis. Non-exam types are single-audience (examiner only).
- **Reference material** ~45K chars from PSIA-RM Assessment Guide 25-26 and Alpine Trainer Program Guide, plus a hand-built Physics/Biomechanics section (~8.5K).
- **Chris's inputs:** Development Assessment (Progress tab — ground truth), per-session comments (MA History thread), Coaching Notes (Themes tab — steers the sparring AI), Checkpoints (periodic review).
- **Scorer output shape** the UI already parses: `scores.{describe,cause_effect,evaluate,prescription,biomechanics,communication}`, `score_rationale.{...}`, `did_well[]`, `opportunity[]`, `key_learning`. **Preserve this shape.** Add fields; don't remove.

The Google Sheet stays as operational data. Chris can read it, it works, and the UI depends on it.

## 3. Why the revamp

The scorer carries all knowledge inside one prompt (~25K chars) and is asked to do everything at once: be a biomechanics expert, apply Fitts & Posner nuance, apply Chris's calibration, score six criteria, write rationale, avoid four named errors, check top-down, verify with Bloom's, handle six MA types. Every instruction competes for attention. Accuracy issues (systematic anchoring at 3 when the rationale describes 4-level work) have been patched with more instruction text, which is lossy and drifts.

Root cause: knowledge lives in the prompt, and the model scores while reading rather than after seeing the whole picture. Chris's corrections — the actual training signal — are being spent as prompt edits instead of stored as examples.

Zoom call transcripts from Chris (~50–60K chars each, ~20/season) make the in-prompt approach impossible regardless.

## 4. Architecture

```
OPERATIONAL DATA          Google Sheet (unchanged)
                          sessions · journal · config · profiles
                                    │
KNOWLEDGE STORE           Supabase Postgres + pgvector
                          uniform chunks: text · source · tags · embedding
                                    │
INGESTION                 one pipeline per source → same chunk schema
                          PSIA docs · Zoom transcripts · Chris comments ·
                          Chris blind scores · curated web · assessments
                                    │
CONTEXT ASSEMBLER         Vercel function, fixed token budget,
                          reserved slots, Chris-privileged ranking
                                    │
REASONING                 Step 1 Extract (Sonnet) → Step 2 Evaluate (Opus)
                          per-criterion, justification-per-level, cited
                                    │
FEEDBACK LOOP             Chris scores blind → stored as exemplar chunk
                          agreement rate tracked per criterion
```

Named pattern: **RAG-grounded LLM-as-a-judge with expert-in-the-loop calibration.** Hybrid search (tags + vectors), query transformation (extraction as query), citation, supersession, active-learning feedback.

## 5. Knowledge store

### 5.1 Chunk schema

```sql
create table chunks (
  id            uuid primary key default gen_random_uuid(),
  text          text not null,
  source        text not null,      -- see 5.2
  source_ref    text,               -- doc section / zoom date / session id
  criteria      text[] not null default '{}',   -- see 5.3
  skills        text[] not null default '{}',   -- see 5.4
  type          text not null,      -- see 5.5
  author        text,               -- 'chris' | 'gates' | 'mike' | 'psia' | 'mark' | null
  date          date,
  approved      boolean not null default false,
  superseded_by uuid references chunks(id),
  metadata      jsonb default '{}',
  embedding     vector(1024),       -- dimension set by embedding model, see 11
  created_at    timestamptz default now()
);
create index on chunks using ivfflat (embedding vector_cosine_ops);
create index on chunks (source, approved) where superseded_by is null;
```

### 5.2 Sources

| source | What | approved default |
|---|---|---|
| `psia_doc` | Assessment Guide, AT Program Guide, curated physics | true |
| `chris_zoom` | Extracted calibration statements from call transcripts | **false** — Chris approves |
| `chris_comment` | Per-session feedback from MA History thread | true |
| `chris_score` | Blind score + note + linked session transcript | true |
| `mentor_assessment` | Development Assessment snapshots | true |
| `web` | Curated research Mark chooses to ingest, with URL | true |
| `past_session` | Scored session extractions, as retrieval exemplars | true |

Gates and Mike use the same sources with `author` set accordingly. Chris is privileged in ranking (see 7.3); others are treated as reference.

### 5.3 Criteria tags

**Scored criteria (2026 AT MA/TU Assessment Form, confirmed by Chris 2026-09-17):**
MA section — `cause_effect` · `evaluate` · `prescription` · TU section — `desired_performances` · `biomechanics` · `equipment`.
Pass rule: MA section average ≥ 4 **and** TU section average ≥ 4.

**Tag-only (not scored, still useful for retrieval):** `describe` · `communication` · `general`.
Describe and Communication were scored lines on the pre-2026 form; the current form folds them into Cause and Effect / Prescription. Chunks keep the tags so the store can still retrieve on them.

### 5.4 Skill tags

`edging` · `pressure` · `rotary` · `balance_fore_aft` · `balance_lateral` · `ski_to_ski` · `upper_lower_separation` · `turn_shape` · `turn_phase` · `dirt` · `conditions` · `tactics` · `equipment` · `intent_verification` · `peer_dialog` · `idp_task` · `physics_sidecut` · `physics_forces` · `physics_camber` · `fitts_posner` · `l3_vs_at`

Tag generously at ingest. Tags are a pre-filter, not the ranking.

### 5.5 Types

`definition` (what a criterion or concept is) · `principle` (how Chris thinks about scoring) · `correction` (Chris disagreeing with a score, with reason) · `exemplar` (a scored session + score + reason) · `physics` (mechanism explanation) · `mental_model` (pendulum, three-joint, etc.) · `idp_task` (task description with ski/body performance)

### 5.6 Supersession

Chris's views evolve. A chunk marked `superseded_by` is excluded from retrieval. Rules:

- Approval view for Zoom chunks asks: "Does this replace an earlier statement?" and shows candidates (same criteria/skills, same author, older). Chris links or skips.
- Saving a new Development Assessment supersedes the prior `mentor_assessment` chunk automatically.
- A `correction` never supersedes a `principle` — it refines. Both retrievable.
- When two approved, non-superseded Chris chunks conflict at retrieval time, the assembler keeps the more recent and logs the pair for Chris to resolve.

## 6. Ingestion pipelines

All pipelines emit chunks in the 5.1 schema, embed, and insert. Nothing else differs downstream.

### 6.1 PSIA documents (one-time, then on update)

Chunk by natural unit, not by token count:
- Scorecards → one chunk per criterion per level (tag criteria + `definition`)
- L3 vs AT comparison → **paired rows kept together**; never split L3 from its AT counterpart (tag `l3_vs_at` + the relevant criterion)
- Fundamentals & Skills Concept → one chunk per fundamental, one per blend explanation
- IDP tasks → one chunk per task including ski performance and body performance (tag `idp_task` + skills)
- Physics section → one chunk per concept, with 1–2 sentence overlap where a concept depends on the previous one
- Assessment flow, professionalism, program overview → **do not ingest for scoring.** Keep in the Resources tab for reading, not in the store.

Exclude clinic scorecards, module LEs, L3 teaching/skiing scorecards from the store.

Model: Sonnet for chunk-boundary decisions; embedding model per §11.

### 6.2 Zoom transcripts (per call)

Input: transcript text. Output: pending chunks for Chris's approval.

Extraction prompt (Opus) pulls discrete calibration statements. For each:
- `text`: the statement, lightly cleaned, in Chris's words
- `criteria`, `skills`: tagged
- `type`: principle / correction / mental_model / physics / exemplar
- `metadata.context`: 1–2 sentences of surrounding conversation so Chris can judge whether it was a firm view or thinking-out-loud
- `metadata.timestamp`: position in transcript

Extraction rules:
- Pull only statements Chris makes about scoring, what he listens for, why something is a level, mental models, corrections of Mark. Not Mark's statements. Not logistics.
- Do not merge two statements into one chunk. Do not generalize beyond what was said.
- Flag statements phrased as hypotheticals or questions — Chris decides.
- Target: 20–60 chunks per hour-long call.

Implementation notes (session 2): pending chunks are stored **without an embedding**; the embedding is written in the same update that sets `approved = true`, and `match_chunks` requires a non-null embedding — an unapproved chunk is unretrievable twice over. Speaker labels are a prior only: the extractor attributes by content, and any chunk whose cited turns are not labelled as the mentor carries `metadata.attribution.check` for the approver. Every chunk carries a verbatim `quote`; chunks whose quote is not in the transcript, or whose cleaned text drifts from the cited turns, are rejected in code before they reach Chris. Text the mentor reads aloud (Mark's notes, the form, app output) is not his statement. Remarks about the app / AI peer / AI scores are returned as `tool_feedback` and **not stored** — they are bug reports, and a stored "the AI scores high" would go stale and bias every later score. Approved chunks are immutable (supersede, don't edit); only the author approves or edits; re-ingesting a call replaces its pending chunks and never touches approved ones. Extraction is not repeatable even at temperature 0 (one run found the 10:16 gear correction, the next dropped it), so each window is read **twice and the passes unioned**; a dry run is a preview, never a record of what will be stored — verify against `/api/chunks/pending`. Score items are checked in code: every form line named must be audible in the cited turns (the extractor once produced a "Describe 2" Chris never gave). `sessions: [{from,to,id}]` stamps `metadata.session_id` so §7.3.5 self-exclusion covers spoken feedback too; `exclude: [{from,to}]` drops off-topic stretches deterministically.

Approval view (new, mentor-only, in Checkpoints or its own tab): pending chunks listed with context, buttons Approve / Edit / Delete / "Replaces earlier →". Approved chunks embed and enter the store. Nothing unapproved is ever retrieved.

### 6.3 Chris comments (continuous)

Every new comment in an MA History thread becomes a `chris_comment` chunk, criteria inferred from text, linked by `source_ref` to the session. Approved by default — he wrote it deliberately.

Implementation notes (session 6): `appendFeedback` — the one writer of the thread — calls `/api/ingest/comment` after the Sheet write, fire-and-forget; the endpoint re-reads the comment **from the Sheet** by timestamp (the thread is the record; nothing a client posts goes straight into the store). Comments over ~600 chars are split into single points on the blank lines he typed — verbatim, grouped never reworded, lead-ins ride forward and short tails ride back; tags are keyword tags, no model call. A comment is skipped if the store holds *any* chunk (live or superseded) with its timestamp, so bootstrap's whole-comment chunks and the hand-split 9/17 comment are never doubled. Scorecards in the thread (either shape) are not comments — §6.4 owns them. `npm run ingest:scores -- --comments` catches up anything the hook missed.

### 6.4 Chris blind scores (continuous — the feedback loop)

See §9. Each submission creates one `chris_score` chunk per criterion where a note exists, plus one `exemplar` chunk containing: session extraction (§8.1) + Chris's six scores + his note. Exemplars are what make the scorer sound like Chris.

Implementation notes (session 6): the scorecard is read from the Sheet by `mentorScores()` (`/lib/mentorScores.js` — the same reader the screen uses; structured blind items and hand-posted lines both count), never taken from the request. **Exemplar text** is organised by form line — what Mark said (rendered from the extraction's own fields: connection counts and each chain flagged complete / partial-with-what's-missing / prompted, the two comparisons, the prescription chain, equipment flags), then the mentor's number — capped at 1.8K tokens so three fit the slot; it carries **no AI score, rationale, or rewrite**. It is **embedded on `extractionToText(extraction)`**, the same function that builds the query, not on the rendered text (`embed_text`, `store.insertChunks`) — like is compared with like, and his numbers don't compress exemplars into one similarity band (the session 1 finding). The blind form has one note field, so `chris_score` note chunks are the note split into single points (same splitter as comments), each tagged to the lines it speaks to and stating what he gave them; they ride in the Chris slot. Metadata holds the full extraction, `blind`, and **`ai_at_submit` — pinned at first ingest and never moved by a rescore or re-ingest**. Idempotent; a changed scorecard or a re-extraction (`--reextract`) replaces the exemplar by supersession; exactly one live exemplar per mentor per session. Chris's exemplar supersedes the session's `past_session` chunk (§6.7); Gates's and Mike's are stored (inter-rater data) but supersede nothing and are not retrieved as exemplars.

### 6.5 Development Assessment (on save)

Snapshot as `mentor_assessment` chunk; supersede prior.

### 6.6 Curated web (manual)

Mark pastes text + URL. Chunk by paragraph, tag, `source_ref` = URL. Not live search — live search is non-repeatable and would make the same session score differently on different days.

### 6.7 Past sessions (batch, then continuous)

Every scored session's Step 1 extraction becomes a `past_session` chunk with the AI scores in metadata. Once Chris has blind-scored it, the `exemplar` (6.4) supersedes it for retrieval — Chris's score is truth, AI's is not.

## 7. Context assembler

Vercel function. Input: task type + session extraction. Output: assembled prompt.

### 7.1 Budget

Total ~30K tokens for the evaluate step. Fixed. Never grows.

| Slot | Reserved | Fill rule |
|---|---|---|
| Criterion definitions | ~2K | Always. All six criteria, all levels. From `psia_doc` + `definition`. |
| Chris chunks | up to ~8K | Top-k by similarity to session extraction, `author = chris`, approved, not superseded. Minimum 5, maximum ~25. |
| Exemplars | ~6K | 2–3 `exemplar` chunks most similar to this session. Prefer same MA type. |
| PSIA reference | remaining (~8–10K) | Top-k `psia_doc` chunks by similarity, filtered by skills present in extraction. |
| Session extraction | ~4K | The Step 1 output. Always. |
| Reasoning template | ~3K | Static. See §8.2. (2K → 3K in session 4 for unit definitions; total unchanged, PSIA absorbs it.) |

If Chris chunks under-fill, PSIA reference takes the slack **up to a 12K cap** (session 4: with 9 Chris chunks and no exemplars, uncapped slack would hand PSIA ~20K of marginal matches). If over budget, trim PSIA reference first, exemplars second, never Chris chunks or definitions.

Definitions are *fetched* by `source_ref` prefix (`at-ma-tu-assessment-form-2026.md#`), not searched. Exemplar slot takes **Chris-scored exemplars only** (`source = chris_score`, `author = chris`, `type = exemplar`); `past_session` chunks carry old-scorer numbers and are never shown to the evaluator as examples. Exemplars are excluded from the Chris slot *in the query* (type filter), so a growing pile of them never uses up that slot's candidates. The exemplar block's header carries the anchoring instruction (find the closest exemplar line, start from Chris's number, move only for a nameable difference in evidence; an exemplar is evidence of level, never of content). `manifest.exemplar_sessions` lists which sessions were shown.

### 7.2 Query

The **Step 1 extraction** is embedded as the retrieval query, not the raw transcript. Extraction is normalized (skills named, physics concepts explicit, phases located) so it retrieves on substance. Raw transcripts retrieve on conversational noise.

### 7.3 Ranking policy

1. Hard filters: `approved = true`, `superseded_by is null`, skills overlap with extraction (for psia_doc; Chris chunks skip skill filter — his general principles should surface).
2. Cosine similarity rank within each slot.
3. Chris slot filled before PSIA slot. This is the architectural guarantee that Chris is ground truth.
4. Recency tiebreak within Chris slot.
5. **Self-exclusion:** chunks whose `source_ref` is `session:<id>` of the session being scored are dropped. Chris's comment on a session must not inform that session's AI score, or agreement (§9) measures retrieval instead of judgment. `includeSelf` exists for A/B debugging only.

### 7.4 Citation

Every retrieved chunk carries its `id` into the prompt as `[c:<short-id>]`. The evaluate step must cite which chunks informed each criterion's score. See §8.3.

## 8. Reasoning — two steps

### 8.1 Step 1 — Extract (Sonnet)

Input: session transcript (structured sections if present), MA type, context (who/activity/conditions).
Output: JSON inventory. **No scoring, no judgment.**

```json
{
  "ma_type": "at_exam | written | scenario | reverse | compare | video",
  "observations": [{ "phase": "...", "ski_performance": "...", "body_performance": "...", "dirt": "..." }],
  "skills_referenced": ["edging", ...],
  "physics_concepts": [{ "concept": "...", "explicit_or_implied": "...", "quote": "..." }],
  "cause_effect_chain": ["A → B → C ..."],
  "primary_fundamental_named": "... | null",
  "intent_verification": { "asked": true, "questions": ["..."], "peer_answers": ["..."] },
  "conditions_considered": "... | null",
  "prescription": { "task": "...", "rationale_to_examiner": "...", "delivery_to_peer": "...", "peer_restated": "quote | null" },
  "examiner_probes": [{ "question": "...", "mark_answer": "...", "deepened": true }],
  "unprompted_vs_prompted": { "unprompted": ["..."], "prompted_creation": ["..."], "prompted_refinement": ["..."] },
  "verbatim_key_phrases": ["..."]
}
```

**Extract v4 (session 6), additive to the schema above:** `comparison_to_intended_outcome` is two comparisons — `vs_intent` (observed performance against the skier's own stated intent) and `vs_ideal` (intent and/or performance against what the task requires) — each `absent | partial | made` with `reference_quote`, `quote`, `prompted`. "Made" without the reference quoted is demoted in code (`comparisonFacts`, `/lib/extractStats.js`), which also still writes the v3 fields (`made`, `quote`, `intent_referenced_quote`) and adds `both_made`; a stored v3 object is lifted on read. `equipment.states_effect_on_observed_performance` separates an effect on what this skier did from a recommendation or its predicted benefit. `prescription.delivery_to_peer` is Mark's words only, and `speakerGuard` checks it against the session's speaker labels — the peer's restatement is moved back to `peer_restated` and logged in `extraction_guards`.

Why a separate step: when the model reads and scores simultaneously it pattern-matches criteria as it reads and lands on "didn't check every box." Inventorying everything demonstrated *before* judging forces whole-picture evaluation. This enforces "evidence descriptors, not checklists" by structure rather than instruction.

### 8.2 Step 2 — Evaluate (Opus)

Input: assembled context (§7) — extraction, definitions, Chris chunks, exemplars, PSIA reference, template.

Per criterion, the template requires **justification-per-level before the score**:

```
For each criterion:
  "Case for 5": what an examiner would say to defend a 5 for THIS work
  "Case for 4": ... 
  "Case for 3": ...
  Then: which case holds, and why the others don't.
  Cite chunks: [c:...] for the Chris statements / definitions / exemplars that informed this.
```

Building the case at each level for the specific session — rather than checking criteria and assigning a number — is the reasoning structure that prevents anchoring. It comes from Mark's justification-engine spec; kept per-criterion so Chris validates at the level he gives feedback.

Standing calibration rules carried into the template (from the current scorer; prune as exemplars accumulate — see §13):
- Criteria are evidence descriptors, not checklists. Depth of work determines level, not whether every term appears.
- Opportunities describe where to focus next; they never lower the current score.
- Prompted refinement (Mark showed it, examiner deepened it) is additive. Prompted creation (examiner had to surface it) caps at 4.
- Locate gaps on the scale: a 4→5 gap does not make a 3.
- Fitts & Posner as Chris thinks: does it appear? is it consistent? 4 = appears regularly.
- Implied physics through accurate mechanism description counts as demonstrated.
- Communication is type-aware: two audiences for AT Exam; examiner-only for all other types — never penalize missing peer delivery where none exists.
- Concise, precise, expert-to-expert delivery is higher than verbose coverage. Phase-by-phase is one way to be clear, not a requirement.
- Metacognition need not be stated; the behavior is the evidence.
- Cognitive-level check: Apply ≈ 3, Analyze ≈ 4, Evaluate/Create ≈ 5. If the cognitive level contradicts the score, the cognitive level wins.

Floor rules (added session 4 — the rules above all guard against scoring low; Chris's first scorecard showed the old scorer 1–2 points **high** on every line). If the case for 3 fails, the template requires a case for 2 and for 1.
- Naming is not connecting: fundamentals mentioned without a stated link to ski performance and an outcome are not cause-and-effect evidence.
- A line not addressed scores 1. `equipment.addressed = false` ⇒ Equipment 1 (also enforced in code, logged in `quality.guards_applied`).
- "Beginning to appear" is a 2, not a 3. Accurate-but-generic is L3 work. Volume is not depth.
- Score against the essential elements each form definition names, not general competence.

Scale and units (v2-rag-3, session 6 — applied with exemplar ingestion, against both scorecards): **one complete instance standing alone is a 3 at most**; 4 needs the element to recur, or — on a line naturally said once (the evaluate comparison, prescription, desired performances, equipment) — one instance complete in every link *and* threaded into the rest of the analysis. Evaluate's complete unit is both comparisons made; one of two is partial. Equipment: a recommendation or predicted benefit is partial at most. `gap_to_next`'s X is something Mark said, never the peer's or examiner's words. With exemplars in the context the evaluator must write `exemplar_anchor` per line (closest exemplar line, Chris's number, weaker / equal / stronger and in what); more than one away from the closest exemplar line needs a named difference in evidence. The evaluate unit's two-comparison *structure* is in the template because the evaluator has to read the v4 fields; Chris's own wording of it is a pending Zoom chunk and, once he approves it, arrives by retrieval and outranks the template.

Chris's own content (X→Y→Z frame, peer piece as coaching cue "plus", the equipment trio, the prescription chain) is **not** in the template. It lives in the store as seven single-point chunks and arrives by retrieval, cited.

### 8.3 Output (backward compatible)

```json
{
  "scores": { "describe": 4, "cause_effect": 4, ... },
  "score_rationale": { "describe": "...", ... },
  "justifications": { "describe": { "5": "...", "4": "...", "3": "..." }, ... },
  "citations": { "describe": ["c:a1b2", "c:c3d4"], ... },
  "did_well": ["..."],
  "opportunity": ["..."],
  "key_learning": "...",
  "gap_to_next": { "describe": "Instead of X, say Y because Z", ... },
  "time_note": "..."
}
```

`justifications` carries `"2"` and `"1"` keys when the case for 3 fails. Also additive (session 4): `section_averages`, `meets_standards`, `citation_details` (author/date/text per cited chunk, for §12.2), `chris_conflicts`, `quality{citations_invalid, criteria_without_citation, guards_applied}`, `extraction`, `meta{scorer, models, ms, context manifest}`. Session 6, additive: `exemplar_anchor{line: "…"}`, `quality.exemplars_in_context[]`, `quality.exemplars_cited[]`, `quality.exemplar_lines_anchored`, `quality.extraction_guards[]`.

`scores`, `score_rationale`, `did_well`, `opportunity`, `key_learning` — existing fields, unchanged shape. `justifications`, `citations`, `gap_to_next`, `time_note` — new. Existing `parseSummary` keeps working.

**Criteria in `scores` (decided 2026-09-17):** the six from the 2026 form — `cause_effect, evaluate, prescription, desired_performances, biomechanics, equipment` — plus `section_averages: { ma, tu }` and `meets_standards: boolean`. The legacy keys `describe` and `communication` remain in `scores` for history and for `parseSummary`'s `hasFullScores` check (the evaluator still emits them, as diagnostic tags rather than exam lines; UI decides whether to show them). Additive: nothing removed.

## 9. Feedback loop — Chris scores blind

**The AI score is hidden until Chris submits.** He currently sees the AI number first and reacts — that's anchoring, and it means "no objection" can't be distinguished from "agrees."

Blind form (MA History, mentor view, replaces the "AI analysis" reveal): transcript visible; six 1–6 tap rows; one optional note field; Submit. On submit: AI score revealed alongside, per-criterion delta shown, both stored.

Stored as: `chris_score` chunks (per criterion with a note) + one `exemplar` chunk (§6.4). Progress tab gains an **Agreement** panel: per-criterion exact-match %, within-one %, trend over last 20 sessions.

Agreement rate is the system's truth. It decides when to prune calibration rules (§13), when to escalate the evaluate model (§11), and whether fine-tuning is ever warranted (§13).

Chris still has the free-text comment thread for coaching feedback to Mark. The blind form is for scores.

**Agreement calculation (session 6, `/lib/agreement.js`, pure; `GET /api/agreement?mentor=chris&window=20`).** (1) The AI number compared is the one the mentor was blind to: his submission's `ai_at_submit`; else the score pinned in the exemplar's metadata at first ingest; else — flagged `unpinned` — the current summary. (2) 2026 form only; a session whose AI score is an old-scorer result is listed under `excluded` with the reason. (3) **Blind and not-blind scorecards are tallied separately and the headline — and the §1 targets — is blind only**: 7n6ry6d (not blind, and the scorer was calibrated on it) is reported but never counts toward the target. (4) A line without both numbers is in neither numerator nor denominator. Output: `blind`, `recent` (last `window` blind cards), `not_blind`, `all` — each with overall and per-criterion n / exact / within-one / % / AI-over / AI-under / signed mean Δ / same-result — plus `trend[]` per blind session, `by_scorer` (so a template change shows up as its own series), `sessions[]`, `excluded[]`. The response contains AI numbers: session 8's panel must request it only for a viewer who is unsealed on every row, or with `mentor=<viewer>` (every row is then a session he has scored).

## 10. Module layout

New code is new files. **The old JSX is read only to harvest bug fixes when rebuilding a tab.** Never extend it.

```
/src/                       NEW frontend, one component per tab
  App.jsx                   shell, login, tab nav
  api.js                    fetch wrappers for /api/*
  components/               Card, SectionLabel, MicButton, SpeakButton, etc.; ScoreViews.jsx draws a scorecard (chips, grid, verdict) and does no arithmetic
  tabs/
    Sparring.jsx            + ATExam.jsx, WrittenMA.jsx as sub-components
    MAHistory.jsx           + BlindScoreForm.jsx, Rationale.jsx, CoachingPanel.jsx
    Journal.jsx             + JournalEntryForm.jsx, JournalThread.jsx (depth assessment + comments)
    Progress.jsx            + AgreementPanel.jsx, ChunkApproval.jsx (session 8: assessment ×4 fields, AI-suggested assessment, trends, recurring gaps, §12.3, §12.4)
    Themes.jsx  Checkpoints.jsx  Videos.jsx  Growth.jsx
    Timeline.jsx  Clinics.jsx  Resources.jsx  Profile.jsx
  lib/
    parseSummary.js         harvested from old app, with all retry logic
    scorecard.js            THE reader of scores: scorecard(session, {viewer}) — forms, averages, verdict, mentor scorecards, deltas, blind seal. Nothing else reads summary.scores.
    prompts.js              sparring/coach system prompts (moved from JSX)
    journal.js              Journal vocabulary (types, prompts, domains, depth), row ↔ entry mapping + guards, contentRow (candidate-owned columns only), applyPulse / mergeComment, listSummary, drafts. Pure.
    resources.js            AT Program Guide learning experiences (harvested verbatim); Journal links to them, the Resources tab (session 10) lists them
    progress.js             Progress-tab logic, pure: ASSESSMENT_FIELDS (4) + mergeAssessment (per-mentor merge into the live Config blob), trendRows / lineSeries / gapHistory over scorecard(session,{viewer}) — a sealed session contributes nothing —, agreementView, groupPending / PRIORITY, assessmentContext (what the AI-analysis prompt may see), access-code store
    cites.js                citation readers (citesFor, mentorCites, exemplarCites, statement) — moved out of Rationale.jsx so scripts can import them

/api/claude.js              existing — sparring/chat calls, unchanged
/api/sheet.js               existing — unchanged
/api/score.js               NEW — one-shot extract → assemble → evaluate (CLI/scripts)
/api/score/extract.js       NEW — Step 1 only; the app calls this, then evaluate (each call well under the 300s limit)
/api/score/evaluate.js      NEW — assemble + Step 2, takes the extraction; with `lines: [key]` + `passage` it re-scores one line (practice, read-only)
/lib/http.js                NEW — shared handler helpers; returns §8.3
/api/ingest/psia.js         NEW — accepts doc text + section type, chunks, embeds, inserts
/api/ingest/zoom.js         NEW — accepts transcript (+ speakerMap; without one returns a speaker report only), runs extraction, inserts pending chunks
/lib/ingest/zoom.js         NEW — parse → windows → Opus → quote/drift guards → pending; shared by the endpoint and the CLI
/api/ingest/comment.js      NEW — called by `appendFeedback` after a mentor comment lands in the Sheet; re-reads it by timestamp, splits, inserts chris_comment chunks
/api/ingest/score.js        NEW — called by `appendFeedback` after a blind scorecard lands; reads the scorecard from the Sheet, inserts exemplar + chris_score note chunks, supersedes past_session
/api/agreement.js           NEW — §9 agreement, read-only (12 functions now deployed — Vercel Hobby's ceiling; see §17)
/lib/ingest/score.js        NEW — renderExemplar, noteChunks, ingestScore; shared by the endpoint and the CLI
/lib/ingest/comment.js      NEW — splitPoints (verbatim), commentChunks, ingestComments
/lib/mentorScores.js        NEW — the one mentor-scorecard reader (moved from src/lib/scorecard.js, which re-exports it): cleanScore, formMath, formatScoreLine, parseScoreLine, mentorScores, isScoreItem
/lib/agreement.js           NEW — pure agreement math (§9)
/api/chunks/pending.js      NEW — list pending chunks for approval view
/api/chunks/approve.js      NEW — approve / edit / delete / supersede
/api/chunks/search.js       NEW — debug endpoint: query → retrieved chunks (retrieval eval)
/lib/store.js               NEW — Supabase client, chunk CRUD, similarity search
/lib/embed.js               NEW — embedding client, single model, single dimension
/lib/vocab.js               NEW — criteria, skills, phases, outcomes; one source for store, extract, evaluate
/lib/score.js               NEW — extract → assemble → evaluate → normalize; shared by /api/score and the CLI
/lib/assembler.js           NEW — §7
/lib/coaching.js            NEW — pure, shared by API and frontend: parseGap, findOriginal, applyRewrite, mergePassage, unitCheck, briefLines
/lib/extractStats.js        NEW — connectionStats / renderChains, split out of prompts/extract.js so the browser bundle never pulls in the SDK
/lib/prompts/extract.js     NEW — §8.1 template
/lib/prompts/evaluate.js    NEW — §8.2 template
/lib/prompts/zoom.js        NEW — §6.2 extraction template
/scripts/bootstrap.mjs      NEW — one-time: pull Sheet data via /api/sheet, ingest all sources (§13 session 1)
/scripts/zoom.mjs           NEW — CLI for the /api/ingest/zoom path; `--dry`, `--replace`, `--pending`
/scripts/score.mjs          NEW — CLI for the /api/score path; `--compare "2,2,2,2,2,1"` or `--compare chris` (his scorecard from the Sheet) checks exact / within-one; prints which exemplars were shown and cited
/scripts/ingest-scores.mjs  NEW — backfill / catch-up: every mentor scorecard → exemplar, `--comments`, `--reextract`, `--dry`; prints agreement
/scripts/selftest-ingest.mjs NEW — offline: exemplar text, idempotence, pin survives rescore, comment split, agreement reproduces §16's 3/6·5/6 and 5/12·11/12, self-exclusion
/scripts/selftest-journal.mjs NEW — offline: row guards, column ownership against a simulated Apps Script (stale candidate save vs mentor depth/comment, no duplicate on retry, no resurrected row), list summary, notify, Challenge prompt; optional live Journal export
/scripts/rendertest-journal.jsx NEW — server-renders the Journal tab from a live export as Mark, Chris and Gates
/scripts/selftest-progress.mjs NEW — offline: assessment field ownership, trends / gaps through the seal, agreement view over lib/agreement.js, pending grouping, AI-analysis context, reply parser, examiner block, token gate
/scripts/rendertest-progress.jsx NEW — server-renders the Progress tab as Mark, Chris (one session scored) and Gates (none); accepts a live export
/scripts/retrieval-eval.mjs NEW — §14
```

Environment: `SUPABASE_URL`, `SUPABASE_SERVICE_KEY`, `EMBEDDING_API_KEY`, existing `ANTHROPIC_API_KEY`, `APPS_SCRIPT_URL` (migration only), `RESEND_API_KEY` / `RESEND_FROM` (optional), **`MENTOR_TOKEN`** (session 8 — the shared access code checked by `chunks/pending`, `chunks/approve`, `agreement`; unset = open, logged).

## 11. Model assignments

| Task | Model | Reason |
|---|---|---|
| Design, architecture, build, fresh-context review | Fable 5.1 | Judgment-heavy, low volume |
| Step 2 Evaluate | Opus | Nuanced expert judgment; volume is a few/week so cost is negligible. Escalate to Fable if agreement rate stalls below target after exemplars accumulate. |
| Zoom extraction | Opus | Judgment about what counts as a calibration statement |
| Step 1 Extract | Sonnet | Well-defined inventory task |
| PSIA chunk boundaries | Sonnet | Mechanical with light judgment |
| Embeddings | One model, chosen at build, **never switched** (switching means re-embedding the entire store). Voyage `voyage-3` or OpenAI `text-embedding-3-small`. Set `vector(N)` in §5.1 to match. |

Opus reviewing Fable's work is not part of the plan — the less capable model checking the more capable one's architecture doesn't help. Review is Fable in a fresh session against this doc.

## 12. Frontend — new app, same interface, same data

The 5,000-line `ATDevelopmentJournal.jsx` is not extended. A new Vite + React app is built one tab at a time, one component per tab, same visual design, hitting the same `/api/*` endpoints and the same Google Sheet. Both apps work against live data; nothing is migrated. Chris stays on the old URL until the new app has every tab he uses (after session 8), then the old app is retired.

Before each tab is rebuilt, Mark lists what bothers him about the current one. Those get fixed. The builder reads the old tab's code once to harvest bug fixes (parseSummary retries, column-mapping guards, sort tiebreakers, localStorage persistence, per-row storage, hash-diff saves), not to copy structure.

New UI that didn't exist before, built into the new tabs where they belong:

1. **Blind scoring form** — MA History, mentor view. Six 1–6 tap rows + note + Submit; AI score revealed after. Candidate view unchanged.
2. **Citations in rationale** — per criterion, cited Chris statements rendered inline ("Chris, Sep 3: '…'"). PSIA/definition citations collapsed by default.
3. **Zoom chunk approval view** — Progress tab, mentor-only. Pending list with context; Approve / Edit / Delete / Replaces-earlier.
4. **Agreement panel** — Progress tab. Per-criterion exact %, within-one %, 20-session trend.
5. **Rescore** → `/api/score`.

Known constraint: localStorage doesn't cross origins. In-progress exam state on the old URL won't appear on the new one.

## 13. Build order

One project, one sequence. Backend and frontend alternate so each session is one kind of work.

| Session | Type | Builds | Done when |
|---|---|---|---|
| **1** | Backend | Supabase project, §5.1 schema, `/lib/store.js`, `/lib/embed.js` (embedding model chosen and locked), `/scripts/bootstrap.mjs` — pulls from the Sheet via existing `/api/sheet` and ingests: `_REFERENCE_MATERIALS` → `psia_doc` (§6.1), `_MENTOR_ASSESSMENTS` → `mentor_assessment`, `mentorFeedback` + `mentorComments` → `chris_comment`, scored MASessions → `past_session` (runs Step 1 extraction on each); `/api/chunks/search.js` | Search returns sensible PSIA chunks for a test query about edging; a Chris comment and a past session appear for a query about the Ben MA. No manual export from the Sheet. |
| **2** | Backend | `/lib/prompts/zoom.js`, `/api/ingest/zoom.js`, `/api/chunks/pending.js`, `/api/chunks/approve.js` | First real transcript in → pending chunks listed via endpoint with context and tags |
| **3** | Frontend | New Vite + React app shell, login, tab nav, shared styles; **Sparring / AT Exam tab** with all 7 phases, speech (mic + speaker), localStorage persistence, save-to-MA-History | Mark runs a full AT Exam in the new app; session appears in the Sheet |
| **4** | Backend | `/lib/prompts/extract.js`, `/lib/prompts/evaluate.js`, `/lib/assembler.js`, `/api/score.js` | Rescoring the Ben (`bzfutxv`) and Chuck (`q5omsst`) sessions produces cited, per-criterion output in the §8.3 shape on the six 2026 criteria; **rescoring `7n6ry6d` with self-exclusion on lands within-one of Chris's 2026-09-17 scorecard (2/2/2 · 2/2/1) on all six lines.** (Amended 2026-09-17: Mark's hand assessments dropped — Chris's scorecard is the only real ground truth.) |
| **5** | Frontend | **MA History tab** — session list, transcript with speaker colors, scores, rationale with citations (§12.2), comment thread, **blind scoring form** (§12.1) on the six 2026 lines, Rescore → `/api/score/extract` + `/evaluate`, delete. **Coaching (added 2026-09-19):** (a) Coaching panel at the top of each session — the six `gap_to_next` rewrites, Mark's original sentence beside each, cited Chris chunk underneath, labelled as AI suggestions; hidden from Chris until he has submitted his blind score, same rule as the AI score; (b) "Try that line again" — Mark rewrites one passage, that line alone is re-extracted and re-evaluated, result shows whether the unit is now complete; (c) pre-exam brief on the AT Exam setup screen — three lines from the last session's top gaps. | Chris scores one session blind in the new app; AI score reveals after; delta shows. Mark opens a past session, reads the coaching panel, reworks one line, and sees that line re-scored in under a minute. |
| **6** | Backend | `/api/ingest/score.js` (blind scores → `chris_score` + `exemplar`), `/api/ingest/comment.js`, agreement calculation, exemplar retrieval wired into assembler | Chris's three blind scores are retrievable as exemplars; a rescore cites one |
| **7** | Frontend | **Journal tab** — six entry types, adaptive prompts, connection tags, theme tags, mentor depth assessment (change/deselect), comments, Challenge Me | Mark creates one entry of each type; Chris assesses depth; notification email fires |
| **7b** | Backend | **Operational data to Supabase Postgres** (added 2026-09-24 after hotfixes 4–7 showed the Apps Script serialises every read behind the 400 KB MASessions tab and cannot carry a second user). `ops_rows` (one jsonb row per Sheet row, `(sheet,id)` key), `lib/ops.js` (the Apps Script contract: `getAll` / `update` upsert / `create` / `delete` / `notify` / `ping`), `api/sheet.js` rewritten on it (same request/response shapes, function count stays 12), `lib/sheet.js` reads the store directly, `notify` → Resend, `scripts/migrate-sheet.mjs` (copy + row-for-row verify, rerunnable), `old-app/api/sheet.js` drop-in so the old app shares the store, `SESSION7B_RUNBOOK.md`. | Migration diff empty; both apps load from Postgres; a cold login is under 3 s; a journal entry and an MA comment round-trip and are visible from the other app; a rescore reads the session from Postgres. |
| **8** | Frontend | **Progress tab** — mentor assessments, AI analysis, **recurring coaching gaps across sessions** (added 2026-09-19), **Agreement panel** (§12.4), **Zoom approval view** (§12.3) | Chris approves chunks from session 2's transcript; agreement panel shows session 6 data. **Chris switches to the new app after this session.** |
| **9** | Frontend | **Themes** (add/edit/delete/depth), **Checkpoints** (pre-populate, coaching notes sync), **Videos** | Feature parity with old app for all three |
| **10** | Frontend | **Growth**, **Timeline**, **Clinics**, **Resources**, **Profile** | Feature parity; old app retired |
| **11** | Feature | **Examiner Sparring** mode (strict examiner character, one probe at a time, 3–4 exchanges, "OK, thank you" ending) + **dual-examiner Debrief** | Mark runs a sparring session and receives the debrief; saved to MA History |
| **12** | Review | Fresh-session review of all code against this doc; retrieval eval (§14); prune §8.2 calibration rules that exemplars now cover (fallback already retired 2026-09-22) | Review notes appended to §16 |

Sessions 1–2 capture Chris's reasoning before any scoring changes. Session 4 is where accuracy moves. Session 5 closes the feedback loop. Session 8 is when Chris moves over.

Each session ends by appending a dated line to §16: built / decided / deferred.

**Parallel, starting now:** Chris records calls; transcripts accumulate for session 2.

## 14. Retrieval evaluation

Scoring accuracy says whether the whole system works. It doesn't say *why* it fails. Separate check:

- Hand-label ten sessions: for each, the 5–10 chunks Chris would want the scorer to see.
- `/scripts/retrieval-eval.mjs` runs assembly for each, reports recall@k against the labels.
- Run after any change to embedding, tagging, or ranking policy.

When agreement drops, retrieval eval tells you whether the assembler pulled the wrong chunks or the model misused the right ones.

## 15. Decisions log

Recorded so they aren't re-argued.

| Decision | Reason |
|---|---|
| ~~Keep Google Sheet as operational store~~ → **Operational data moves to Supabase Postgres (session 7b, 2026-09-24)** | Original reason: works; Chris reads it; UI depends on it. Superseded: hotfixes 4–7 showed the Apps Script serialises every read behind the biggest tab, goes cold between calls, and stacks executions when a slow call is retried; a second user (Chris after session 8) made it fail outright. The knowledge-layer decision stands; now both layers are in the same Supabase project. The Sheet is kept as a dated snapshot until session 10. |
| `/api/sheet` keeps the Apps Script contract exactly; rows are jsonb in one table | Both apps and every lib read strings keyed by column name. Keeping the shape means no tab, lib or script changes; jsonb means a new column never needs DDL and a key is never silently dropped (how the Journal lost `entryType` for months). |
| `create` = `update` (upsert on `(sheet,id)`) | The Apps Script appended blindly and could duplicate a row; both apps only ever `create` after a failed `update`. An id exists once. |
| Reads parallel, writes serial in the browser | Postgres carries the three login loads together; the merge-before-write discipline (re-read, then write one column) still must not race its own retry. |
| Notifications via Resend, optional | `MailApp` went with the Apps Script. No key → `notify` reports it and the save still succeeds; mail is never allowed to fail a write. |
| Supabase pgvector for knowledge | Sheet has no vector search and we've already hit cell limits. Postgres, free tier, Vercel-friendly. |
| No vector store before Zoom transcripts | Everything fit in context. Transcripts (~1M chars/season) break that. |
| Two-step scoring | Enforces evidence-before-judgment architecturally; instruction alone didn't stop 3-anchoring. |
| Per-criterion output, not one holistic score | Chris validates at the criterion level; that's the calibration data. |
| Justification-per-level inside each criterion | Building the case for each level prevents anchoring at 3 and looking up. |
| Chris's chunks privileged in ranking | He is ground truth. Encoded as architecture, not instruction. |
| Blind scoring | Seeing the AI score first anchors. "No objection" ≠ "agrees." |
| Extraction as retrieval query | Normalized; retrieves on substance not conversational noise. |
| Fixed budget, never grows | New data competes for budget instead of breaking context. |
| No live web search in scoring | Non-repeatable; same session would score differently by day. Curate and ingest instead. |
| Zoom chunks require Chris approval | Extraction over-generalizes and misattributes; Chris is the only one who knows what he meant. |
| Opus for evaluate, not Fable | Cost at low volume is negligible either way; start with Opus, escalate on evidence. |
| Fable builds, Fable reviews (fresh session) | Value of review is context reset, not model swap. Opus reviewing Fable is inverted. |
| No fine-tuning now | Zero labeled data. Revisit at 50+ blind scores (§13 session 6 onward). |
| Preserve existing output shape | `parseSummary` and the UI depend on it. Add fields, never remove. |
| New code in new files | The 5,000-line JSX is where tokens go. Don't extend it. |
| New frontend, same interface, same data, tab by tab | Monolith is unmaintainable; shared Sheet means no migration and each tab is live on deploy. Chris switches after session 8. |
| Scored criteria = Chris's 2026 form (six: cause_effect, evaluate, prescription, desired_performances, biomechanics, equipment); describe & communication become tag-only; report MA/TU section averages | The examiner's form is ground truth. Chris confirmed 2026-09-17 that the revised six-criterion form is the current standard. Scoring lines the exam doesn't score trains the wrong thing; not scoring Equipment and Desired Performances hides a third of the exam. Decided 2026-09-17. |
| Evaluate ladder runs both directions; floor rules in the template | The original rules only guarded against scoring low. Chris's first scorecard (7n6ry6d) was 1–2 below the old scorer on every line. Decided 2026-09-17. |
| A session never retrieves its own chunks | Otherwise Chris's comment on a session leaks into its rescore and agreement rate stops meaning agreement. |
| Exemplar slot = Chris-scored only; `past_session` never shown as examples | Old-scorer numbers are known-biased; examples teach level. |
| `general` Chris chunks ride in the Chris slot | They already skip the skill filter, and the slot under-fills for the foreseeable future. Revisit only if Zoom volume crowds it. |
| One scorecard: every score on screen comes from `scorecard(session)`; 2026-form and old-scorer results are never averaged, trended, or compared; a missing line is "—", never 0, and voids its section average | Mark, session 5: "scores have not been consistent across the application." The old app derived the score in eight places, four ways (sum-of-six best attempt, top-level scores, non-zero keys only, legacy keys by name). Decided 2026-09-19. |
| Every MA History write sends one Sheet column (`summary` or `mentorFeedback`), and the comment thread is re-read and merged before it is written | Apps Script `update` is per-column. Whole-row saves from memory let Chris's comment and Mark's rescore overwrite each other. |
| "Try that line again" runs the full ladder on one line, retrieves on the session's ORIGINAL extraction, and is never saved | A shortened ladder is a different scorer; retrieval that shifts with the rewrite makes before/after incomparable; practice is not a session. |
| A submitted blind scorecard is final and carries `ai_at_submit` | A score edited after the reveal is anchored. Agreement (§9) must compare against the AI score he was blind to, even after a rescore. |
| Exemplars are embedded on the extraction text, stored as per-line evidence + Chris's number, and carry no AI output | The query is `extractionToText(extraction)`; embedding the same thing compares like with like, and session 1 showed scores in embedded text compress every session into one band. An exemplar that showed AI numbers or rationale would teach the scorer its own bias back. Decided 2026-09-19. |
| Exemplar slot is Chris's only; other mentors' scorecards are stored, not shown | He is the assessor; the levels the evaluator is taught are his. Gates/Mike cards are inter-rater data. |
| The AI score a scorecard is compared with is pinned once (`ai_at_submit`, else at first ingest) | A rescore — including one made better *by that very scorecard* — must not rewrite history, or agreement drifts toward 100% by construction. |
| Agreement headline and §1 targets count blind scorecards only | A card given after seeing the AI number is anchored; a card the scorer was calibrated on is not a test. Both are reported, neither counts. |
| Knowledge ingestion is triggered by the app but reads from the Sheet | The Sheet row is the record. A client can ask for a session to be ingested; it cannot post a scorecard or a comment into the store that the thread doesn't hold. Failure never blocks the Sheet write; the CLI catches up. |
| An exemplar bounds a score; it never sets it | `v2-rag-3` told the evaluator to start from Chris's number and move for named differences; against a single all-2s exemplar it added levels ("two levels above: 4"). Weaker/equal ⇒ ≤ N, stronger ⇒ ≥ N, and the rung is earned on the scale. Decided 2026-09-19. |
| Template changes are A/B-tested on a fixed extraction (`--reuse`) | Extraction is not repeatable (7uk7lnh: 9 → 10 connections, Evaluate 3 → 4 across runs); a fresh one confounds the comparison. |
| One complete instance alone = 3 at most; 4 = recurs, or (single-occurrence lines) complete and threaded | Two over-scores on 7uk7lnh came from "complete: 1 · partial: 0" having no rung. Applied with exemplars, tested on both scorecards, not tuned line by line. Decided 2026-09-19. |
| Journal writes are split by owner: Mark's save sends the content columns; a depth tap sends `mentorPulse`; a comment sends `mentorComments` — the last two after re-reading the live row; all through `update` (which upserts), never `create` | Same failure as MA History: the old app saved the whole row from memory on every action, so Mark's edit could erase Chris's depth read and two commenters could erase each other. `update`-only means a retried create cannot duplicate a row; the re-read also stops a write to a deleted id from resurrecting an empty row. Decided 2026-09-19. |
| Challenge Me has its own prompt, keyed on the entry's type; it reads no scores and its reply is never written to the Sheet | The old button sent the entire eight-layer sparring prompt (reference text, four transcripts, old-scorer numbers) to challenge a clinic note as if it were an MA. Scores are read by `scorecard()` only (row above). The reply is an AI prompt to think, not a record; it is kept on the device, tied to the text it answered. |
| Mentors are emailed only after the Sheet confirms a NEW entry; the result is shown, with a resend | The old app fired `notify` beside an unawaited save — a mail could announce an entry that never landed. Edits never notify. |
| The old in-prompt scorer is retired: there is no fallback. A failed scoring run is shown as failed and retried; it is never scored another way | Decided by Mark 2026-09-22, pulled forward from session 12. It had produced a labelled but confusing result on the pre-2026 lines, doubled the wait on every failure (the fallback call was the longest through `/api/claude`), and pulled the 47 KB PSIA reference text into the browser bundle. Old-scorer results already in the Sheet still display, labelled, and are still never mixed with 2026 numbers. |
| Backend/frontend sessions alternate | Each session is one kind of work; review is easier; the scoring loop is live by session 6 without waiting for all tabs. |

## 16. Session log

*(append one line per build session: date · session # · built · decided · deferred)*

- **2026-09-24 · Session 8 (frontend — Progress)** · Mark: add where the mentor wants him challenged or pushed. · Built: `src/lib/progress.js` (pure), `src/lib/cites.js` (citation readers out of `Rationale.jsx`), `tabs/Progress.jsx`, `tabs/AgreementPanel.jsx`, `tabs/ChunkApproval.jsx`; `App.jsx` adds the tab for both roles. **Development Assessment:** four fields — the fourth, `challenge` ("Where to challenge or push"), is stored in the same `_MENTOR_ASSESSMENTS` blob, rendered to the `mentor_assessment` chunk by `bootstrap.mjs`, and fed to the examiner through `mentorGapsBlock` ("push him on — …"). Writes are per mentor: re-read the live blob, merge this mentor's four fields, write the one Config column (the old app wrote the whole blob from memory on every keystroke); autosave 1.5 s, no-change → no write, another device's change taken only while nothing is being typed. **AI analysis** (mentor): `ASSESSMENT_SYSTEM` / `buildAssessmentUser` / `parseAssessmentReply` in `prompts.js` — built from `assessmentContext()`: his own scorecards with the AI's beside them on sessions he has scored, the evaluator's gap lines on those (labelled AI), sessions he has not scored give date, task and Mark's words and **no number**; old-scorer numbers never shown; journal with depth reads and comments; four sections, each with an "append to my field" button; nothing saved until it is in his fields. **Trends:** 2026-form cards only via `scorecard(session,{viewer})`, oldest → newest, Chris's number under the AI's where he scored; old-scorer sessions in a collapsed labelled list, never mixed; a mentor sees only the sessions he has scored and is told how many are withheld. **Recurring gaps:** every `gap_to_next` grouped by line, newest first, lowest average first; a line recurs at ≥2 sessions, "same gap every time" when the because-clauses share ≥3 content words, theme = the shared words; the first Chris citation under each; sealed sessions contribute nothing. **Agreement (§12.4):** `/api/agreement` drawn as blind headline (exact / within-one / same result against the §1 targets), not-blind reported and not counted, per-line table with lean and mean Δ, inline-SVG trend over the window, by-scorer line, per-session deltas, excluded list; a mentor can only request `mentor=<himself>`; the candidate switches between Chris / Gates / Mike. **Zoom approval (§12.3):** pending grouped by call, the 42:23 and 49:35 statements starred first (`PRIORITY`, one-off), each with timestamp / type / hedged (+note) / check-speaker / watch / criteria / skills chips, the cleaned text with **Heard:** (verbatim `quote`) and the ASR fixes under it, a bracket warning, context on the call, "Replaces an earlier statement?" candidates with the §5.6 rule stated; Approve (with edits and replaces), Edit (stays pending; type, lines, skills, clears hedged), Delete (confirm), bulk-approve of selected; the candidate sees the list and may delete; decisions tallied per type in the browser (§17 "extraction quality"). **Auth (§17):** `requireToken` in `lib/http.js` — `MENTOR_TOKEN` checked as `X-AT-Token` (or `?token=`) on `chunks/pending`, `chunks/approve`, `agreement`; unset = open and logged, `pending` reports `auth: "open"` and the tab shows it; 401 `{ auth: "required" }` → the tab asks for the code once and keeps it in localStorage; `api.js` sends it on every `/api` call. `chunks/search` gains `trim: true` (no metadata). Function count still 12. · Verified offline: `npm run test:progress` (8 groups — incl. the 9/18 comparison reproduced through `agreementView`, no AI number for a sealed session in trends / gaps / AI-analysis context, token gate open/401/header/query); `npm run render:progress` (16 checks as Mark, Chris, Gates — Chris's page never carries the unscored session's name with a score, agreement locked to himself; Gates sees no number at all); history / exam / score / ingest / zoom / journal / ops selftests; `vite build` clean (333 KB). `AT_Mentor_Guide.md` rewritten for the new app (blind scoring, four fields, approval, agreement, access code) and checked into the repo. · **Not verified here (no keys, no deploy): every live call** — `MENTOR_TOKEN` set on Vercel; Chris entering the code; his approvals of the 37 pending chunks (42:23 and 49:35 first); the agreement panel against the live store (it needs `ingest:scores` to have run — session 6); the AI analysis call. Done-when open until Chris approves and the panel shows the two cards. · Decided: shared `MENTOR_TOKEN` is the session-8 auth (per-user signed login stays in §17); the fourth field goes to the examiner now, to the pre-exam brief and Challenge Me later (§17). · Deferred: see §17.
- **2026-09-24 · Session 7b (backend — operational data to Postgres)** · Mark: MA History keeps failing to load; go step by step. §15 row 1 amended (see the row); §13 row 7b added. · Built: `supabase/schema-ops.sql` (`ops_rows`: `sheet`, `id`, `row` jsonb, `seq`; RLS on, service key only; `npm run migrate` now runs both schema files); `lib/ops.js` — the Apps Script contract on Postgres: `getAll` returns every row with every column (`DEFAULT_HEADERS` ∪ keys seen, `''` when empty, insertion order), `update` merges only the keys sent and creates on a new id, `create` is the same upsert, `delete` hard-deletes and answers `Row not found with id: …`, `notify` reads `_MENTOR_PROFILES` and sends through Resend (`RESEND_API_KEY`, `RESEND_FROM`) or reports "No email provider configured" without failing; values coerced to text (objects → JSON); the live MASessions `b`-for-`id` header folded into `id` on read and never stored (§17 item closed); a memory adapter with the same semantics for tests and dry runs. `api/sheet.js` rewritten on it (Apps Script proxy archived as `archive/sheet-appsscript.js`): 200 `{ error }` for the script's own errors, 502 for a store failure so the client's transient retry still applies, `maxDuration` 30. `lib/sheet.js`: `getAll` reads the store directly when `SUPABASE_URL` is set (scorer, agreement, ingest, bootstrap, CLIs — no HTTP hop, no retries), `{ via: 'http' }` keeps the Apps Script path for the migration. `scripts/migrate-sheet.mjs` (`--dry` / copy + verify / `--verify` / `--from <dir>`): reads the three tabs through the old app's proxy, refuses a tab with duplicate ids, batch-upserts, reads back from Postgres and diffs every row and column, writes `out/migrate-sheet-report.json`. `src/api.js`: reads no longer queue behind each other (writes still do). `old-app/api/sheet.js` — self-contained drop-in (generated by `scripts/build-old-app-sheet.mjs`) so the old app shares the store until session 10. `SESSION7B_RUNBOOK.md` — nine steps, each with a check, rollback at both stages. · Verified offline: `npm run test:ops` (contract shapes; per-column update; create never duplicates; header fill and order; `b` handling both ways; `src/api.js` untouched — stale-copy journal save keeps Chris's depth read and Gates's comment, pulse deselect, deleted-entry guard, MA per-column writes with the `ma_` prefix; `lib/sheet.js` from the store; notify with and without a provider, one bounced address; endpoint status codes); journal / history / exam / score / ingest / zoom selftests; `vite build` clean. · **Not verified here (no keys): every live step.** Done-when is open until Mark runs the runbook: table created, dry run counts match the Sheet, copy with an empty diff, new app on Postgres with a sub-second login, journal + comment round-trip, old app on the same store. · Decided: five §15 rows. Function count 12 (`api/sheet.js` replaced, none added). · Deferred: see §17 (Sheet snapshot retirement; `_REFERENCE_MATERIALS` and other legacy Config rows carried as-is; auth item now also gates writes to Postgres).
- **2026-09-24 · Hotfix 7 (login still failing — cause found)** · Mark's HAR: the three login getAll calls fire together; Config answered in 2.6 s, Journal 5.0 s, MASessions failed with the proxy's own "Apps Script did not answer within 10 s" three times → 504. Not his network (the 504 carries a Vercel id). Reproduced from the container: the same three calls **in parallel** took 12–29 s each and threw a 502; **one after another** they took 1.5–2.5 s each. The Apps Script serialises work on the spreadsheet, and an aborted fetch does not stop the execution on Google's side, so the 10 s × 3 proxy retry (hotfix 4) made it worse: every timeout stacked another execution behind the late one — the biggest sheet was always the one starved past the limit. · Fixed: `src/api.js` runs Sheet requests through a one-at-a-time queue (login now Config → Journal → MASessions in sequence, ~6 s; writes and mentor calls join the same queue, so a retry can never race its own earlier attempt); `api/sheet.js` → 2 attempts × 25 s (a slow-but-alive call is allowed to finish, still inside the 60 s function). Verified: queue test (three parallel loads → one in flight at a time, all resolve; a failure doesn't jam the queue), journal / history / exam selftests, build. · This closes the transport question for now, and it is also the strongest argument yet for moving operational data off Apps Script (proposed 7b): a store that serialises every read behind a 400 KB one cannot carry a second user.
- **2026-09-22 · Hotfix 6 (unsent peer text)** · Mark typed his prescription on the Prescribe screen, clicked "Move to presentation" without **Say**, and saved: session `ma_sjm4bip` reached the Sheet with a prescription section that holds only his restatement of the peer's intent and no task, terrain or why; the typed text was cleared with the exam. The move buttons only checked that ONE message had been sent. · Fixed in `ATExam.jsx`: "Move to prescription" and "Move to presentation" are disabled while the composer holds unsent text, with an amber note saying the peer hasn't heard it; the Presentation screen now shows the delivered prescription for review above the private notes, so what the examiner "heard" is on screen while Mark writes. Verified by server-render (button disabled with pending text, enabled when empty, guard on both screens, review block present); exam selftest; build. · The 9/22 session's prescription is to be appended from Mark's retyped text (two-column write: `sections`, `transcript`) and the session rescored; its date reads 2026-09-23.
- **2026-09-22 · Hotfix 5 (old scorer retired)** · Mark: part ways with the old scorer — it causes confusion and may be slowing things down. Agreed, and it was scheduled for session 12 anyway. Removed: the fallback branch in `ATExam.score()`, `SCORER_SYSTEM` / `buildScorerSystem` / `buildScoreInput` / `SCORE_JSON_SHAPE` from `prompts.js`, the `reference/psia.md?raw` import and the `referenceText` prop (bundle 353 → 287 KB). A failed run now shows one error line and **Score again**; a legacy result never appears for a new run. Display of the 17 legacy-scored sessions in MA History is unchanged (labelled, never mixed). Verified: exam / history / score / journal / ingest / zoom selftests, render of the failed-run panel, build. · §15 row added; §13 row 12 amended; the §17 `api/claude.js` heartbeat item is moot (only short turns go through it now).
- **2026-09-22 · Hotfix 4 (slow history load)** · Mark: MA History retrieval is slow. Measured through `/api/sheet`: a good `getAll` is ~2 s (MASessions 18 rows / 380 KB, Config 47 KB); about one request in four instead hangs 20–70 s and returns Google's HTML "page not found" (the known 302-drops-the-POST failure). The browser retried correctly — but only after the whole hang, and login loads three sheets in parallel, so more than half of logins waited 20–70 s and two hangs in a row meant minutes. Not the payload, not the tab: the wait is upstream. · Fixed in `api/sheet.js`: each upstream attempt is cut off at 10 s (`AbortSignal.timeout`) and retried inside the proxy, up to 3 tries, on timeout / 404 / 5xx / HTML / `Unknown action: `; `maxDuration: 60`. A bad attempt now costs ≤10 s, and the second try has always succeeded in the probes. Response contract unchanged; the browser retry remains as the second layer. Writes are retried the same way the browser already retried them (`update` upserts, so a repeat cannot duplicate; the only `create` in the new app is `saveMaSession`'s fallback after `update` has already failed non-transiently). Verified against a fake Apps Script (HTML 404 → retried → 200; three failures → 404 with the HTML in `error`; good answer untouched). · Still true after this: a cold Apps Script instance is slow on its first call; if 10 s proves too tight for a much larger MASessions tab, raise `ATTEMPT_MS` — the log line names the sheet and the attempt.
- **2026-09-21 · Hotfix 3 (evaluate → HTTP 504)** · After the keep-alive deployed (confirmed live: a zero-cost probe of `/api/score/evaluate` answers 200 with the leading heartbeat byte), Step 1 got through and Step 2 died with 504. Cause: `lib/llm.js` made NON-streamed calls with a 240 s client timeout and one retry. Evaluate took 128 s on 9/18; the models are running about 2× slower today (Step 1: 90 → 179 s), which puts evaluate past 240 s — the SDK cut it off, started over, and the 300 s function limit hit. A 240 s timeout plus a retry can never fit in 300 s; it only worked while calls were fast. · Fixed: `complete()` streams (`messages.stream().finalMessage()`), so the timeout bounds only the wait for the response to start (90 s, one retry for fast failures like 429/529) and a long generation is never cut off mid-way; a 275 s budget aborts with a plain-language error instead of Vercel's bare 504. Request parameters are byte-identical (model, max_tokens, temperature 0, system, user — asserted against a fake SSE endpoint), so scores cannot move; all callers (`score.js`, `extract.js`, `psia.js`, zoom) go through it. Exam screen: a fallback (old-scorer) result now has **Score again with the new scorer** — replaces the fallback, uses no revision — instead of pointing at Revise, which wipes the later phases. · Verified: fake-SSE test (same text, same params, clean timeout), score / ingest / zoom / exam / history selftests, render of the exact on-screen state, build. **Not verified live** (no API key here). · The §17 item on Step 1 duration stands: streaming removes the cut-off, not the 300 s ceiling.
- **2026-09-21 · Hotfix (AT Exam scoring failure)** · Mark finished a test exam; scoring ran for minutes and ended on "Unable to reach the sparring partner right now." Cause of the MESSAGE (found in code): the new scorer threw, the exam fell back to the old single-prompt scorer through `/api/claude`, and that call died too — `api/claude.js` was the one function without `maxDuration` in `vercel.json`, and the legacy scorer prompt is the longest call it ever gets. The screen then (a) showed the fallback's failure text as if it were feedback, (b) hid the new scorer's actual error (the hint only rendered when the fallback had parsed), (c) counted the failure as an attempt, and (d) offered only Revise, which clears the prescription, presentation and debrief. · Fixed: a run where both scorers fail is `{ failed: true, scorer_error, fallback_error }` — not an attempt, never saved (`isFailedRun` in `lib/exam.js`, filtered in `buildSession`), replaced on retry, recognised retroactively in an exam already sitting in localStorage; the scored screen shows both errors and one **Score again** button that re-runs scoring with nothing cleared; Revise/Save hidden while there is no real score; `api/claude.js` → `maxDuration: 300`. Verified: the exact stored state renders the new panel; exam / history / score / journal selftests pass; build clean. · **Root cause, found the same day:** the panel read `New scorer: Failed to fetch` — a dropped connection, not an HTTP error. A server-side probe of `/api/score/extract` on `ma_7uk7lnh` returned 200 after **283 s** (model call 179 s for a 22 KB extraction; the rest was the Sheet read on the `sessionId` path) — Step 1 was ~90 s in session 5. The function finishes; a request that is silent for three minutes does not survive Mark's network path. Fix: `slow()` in `lib/http.js` — after validation the two scoring endpoints commit 200 and write one space every 10 s, then the JSON; errors arrive as `{ error }` in the body (every caller already checks it). Verified over a real HTTP round trip; `lib/llm.js` and all prompts untouched, so scores cannot move. **Not yet verified live.** · (superseded note:) Root cause of the first failure was not known when the UI fix shipped — it is in the `New scorer:` line Mark will see after deploying. Endpoints answer and are configured (probed with empty bodies). · §15 unchanged: the legacy fallback stays, labelled.
- **2026-09-19 · Session 7 (frontend — Journal)** · Mark: the list needs a second line of each entry; otherwise the tab is good. · Built: `src/lib/journal.js` (types, adaptive prompts, domains, depth options and default themes harvested unchanged; `normalizeJournalRow` with the old loader's guards — Config/`ma_` rows skipped, JSON columns parse-or-default and type-checked, a Sheet-reformatted date read back as yyyy-mm-dd — plus a stable id for an id-less row, where the old app minted a new one per load; sort date → timestamp → id; `contentRow` / `createRow`; `applyPulse`, `mergeComment`, `topDepth`; `listSummary`; `notificationFor`, `notifyRecipients`; drafts), `src/lib/resources.js` (the 21 learning experiences, verbatim, so the resource link keeps working before session 10), `tabs/Journal.jsx` (list → entry → edit; theme filter; mentor list flags "Needs your read"; candidate-only edit / delete / Challenge), `tabs/JournalEntryForm.jsx` (six types, prompts follow the type, Conditions field — a Sheet column the old form never exposed, mic on every prompt, connection + theme tags, resource, working copy autosaved per entry with restore/discard, no-change save writes nothing, says who will be mailed), `tabs/JournalThread.jsx` (depth: set / change / tap again to clear, busy + error states; comments with a persisted draft and mic), `prompts.js` += `buildChallengeSystem` / `challengeUserMessage`, `api.js` += `loadJournal`, `saveJournalEntry`, `setJournalPulse`, `appendJournalComment`, `deleteJournalEntry`, `notifyMentors`; `App.jsx` loads the Journal with the rest and adds the tab for both roles. **List summary:** line 1 is the first line of the first answered prompt; line 2 is the rest of that answer when it ran on, else the next answered prompt with its label. · **Found in the live Sheet:** (1) **the Journal tab has no `entryType` column** — the Apps Script writes only keys that match a header and `autoCreateTab` never made one, so the old app has never stored an entry's type (all four live rows reload as coaching). The tab detects this from the row keys, tells Mark on the list and in the form, and does not label an untyped entry as Coaching. One header cell fixes it; no code. (2) `_MENTOR_PROFILES` holds one profile, `chris`, whose email is Mark's Gmail (left from the May test in `_NOTIFY_DEBUG`) — the notification would fire and reach Mark. (3) `Config._THEMES` is empty; entries are tagged with the old app's hard-coded default theme ids, so the same defaults are the fallback here. · Verified: `npm run test:journal -- out/Journal.json` (10 groups; simulated Apps Script with the real header-mapping / per-column / upsert semantics: Mark's save from a stale copy keeps Chris's depth read and Gates's comment, two stale commenters both land, deselect reaches the Sheet, a transient failure is retried and a repeated create leaves one row, a depth tap on a deleted entry creates nothing, the type is dropped without the header and round-trips with it; all four live rows survive load → save field for field); `scripts/rendertest-journal.jsx` against the live Journal + Config export as Mark, Chris, Gates; history / exam / score / ingest / zoom selftests pass; `vite build` clean. · **Not verified here: any live write, the Challenge call, the email.** Done-when open on all three counts — six entries (after the header), Chris's depth read (he is away), the email (after his address is in his profile). · Decided: three §15 rows. No new serverless function (still 12). · Deferred: see §17.
- **2026-09-19 · Session 6c — live check on fixed evidence (`--reuse`)** · 7n6ry6d unchanged: exact 4/6, within-one 6/6. 7uk7lnh: 3/4/3 · 3/3/2 vs Chris 2/2/3 · 2/3/3 — exact 2/6, within-one 5/6. **Fixed:** anchors now read "bound: ≥2", no level arithmetic; Equipment 4 → 2 (guard fired: every equipment sentence is a recommendation; Chris 3, now −1 where it was +1). **Not fixed: Evaluate 4 (Chris 2)** — no private-notes guard fired, so the ideal was spoken; both comparisons are genuinely made, unprompted; the evaluator judged the single instance "threaded". This is the residual predicted in the 6b entry: the saved blind score under `v2-rag-2` was also 4. Desired Performances 3 (+1) follows from the same spoken ideal (earlier extractions missed it) with `fundamentals_blended: []`. · Totals on these two cards — 6a: 6/12 exact · 12/12 within one (different extraction); 6c: 6/12 · 11/12. Indistinguishable at n = 2. · **Decided: stop tuning.** One blind line on one session is not a basis for another template change; every further edit tonight would be fitting it. Ship 6c because its changes are each verified against something other than the score (Chris's 9/18 equipment ruling, speaker labels, the anchor text), not because the totals moved. Template frozen at `v2-rag-4` / extract v4 until the third scorecard, which must be scored blind by this scorer before Chris sees it. · Follow-up: re-ingest 7uk7lnh's exemplar after 6c so its equipment line is rendered from the guarded extraction (it currently says "states the effect: yes" beside Chris's 3).
- **2026-09-19 · Session 6 — live runs, 6b failed on the blind session, 6c** · Ingest: both exemplars live (first run was accidental — `--dry.` with a trailing period was silently ignored; CLIs now reject unknown flags). **6a, exemplars alone (`v2-rag-2`):** 7uk7lnh 3/3/3 · 2/3/4 vs Chris 2/2/3 · 2/3/3 — exact 3/6, within-one 6/6 (saved blind score was 5/6; Evaluate 4 → 3); 7n6ry6d 2/1/3 · 1/2/1 vs 2/2/2 · 2/2/1 — exact 3/6, within-one 6/6. Total 6/12 · 12/12 (before exemplars 5/12 · 11/12). Each rescore was shown the other session's exemplar and cited it — the "a rescore cites one" half of done-when is met. **6b (extract v4 / `v2-rag-3`):** 7n6ry6d 2/2/3 · 1/2/1 — exact 4/6, within-one 6/6, Evaluate 1 → 2 through `vs_intent: partial` (the three-state comparison doing its job). **7uk7lnh 3/4/3 · 3/3/4 — exact 2/6, within-one 5/6, Evaluate +2: FAIL.** Diagnosis from the extraction and `exemplar_anchor`: (1) the anchor instruction ("start from Chris's number, move for a named difference") became an adder — with one exemplar, the all-2s session, every line was "stronger than a 2" and the evaluator wrote "Warrants two levels above: 4", "three levels above: 4"; (2) the v4 clerk said yes to every new field: `states_effect_on_observed_performance: true` on a quote whose every equipment sentence is should / might allow / will help (Chris, 9/18: "a recommendation"), and `vs_intent.reference_quote` was the peer's sentence, not Mark's; (3) each run re-extracted, so the A/B was confounded. · **6c built:** `evidenceGuards` in code (replaces `speakerGuard`): evidence whose quote exists only in private notes is cleared (in notes *and* spoken = spoken); an equipment quote whose every equipment sentence is modal/future is not an effect on observed performance; a `vs_intent` reference that is someone else's words is flagged, state left alone; runs before `comparisonFacts` so a cleared reference demotes "made". `whoSaid()` in `lib/coaching.js`. **`v2-rag-4`: an exemplar BOUNDS a score (weaker/equal ⇒ ≤ N, stronger ⇒ ≥ N); the rung above is earned on the scale, never "N plus the differences"**; cite an exemplar only where it bounded. `score.mjs --reuse` re-evaluates the saved extraction (current guards re-applied) so a template change is tested on fixed evidence. Offline tests use the real 7uk7lnh quotes. · **Open — Evaluate on 7uk7lnh is not fixable from presence alone.** Mark did make both comparisons, unprompted, in outcome terms; Chris gave 2. What separates his 2 from a 4 is in his own pending statement (42:23: "go to the component parts… always in relationships between fundamentals, phase to phase") — the *depth* of the comparison, not its existence. That is Chris's content and arrives by retrieval when he approves it; it is not going into the template. Expect Evaluate +1 on this session until then. · Live check of 6c pending.
- **2026-09-19 · Session 6 (backend — the feedback loop)** · Built, as two commits so each can be measured on its own. **6a (exemplars alone, scorer unchanged `v2-rag-2`):** `lib/mentorScores.js` (the one scorecard reader, moved out of `src/lib/scorecard.js`, which re-exports it; `vocab.MENTORS`); `lib/ingest/score.js` + `api/ingest/score.js` (scorecard read from the Sheet → per-line exemplar ≤1.8K tok embedded on the extraction text + note chunks; AI score pinned once; idempotent; replaces by supersession; Chris's supersedes `past_session`); `lib/ingest/comment.js` + `api/ingest/comment.js` (verbatim single-point split, seen-by-timestamp guard); `lib/agreement.js` + `api/agreement.js`; `store.js` += `embed_text`, ids returned from `insertChunks`, `liveBySourceRef`, `commentTimestamps`, `listExemplars`; assembler: exemplar slot `author = chris`, exemplars excluded from the Chris slot in the query, anchoring header, `manifest.exemplar_sessions`; `src/api.js` `appendFeedback` → fire-and-forget ingest with one retry; `scripts/ingest-scores.mjs`, `score.mjs --compare chris`; `Rationale.jsx` draws a cited exemplar as "Compared with a session Chris scored…", never inside quotation marks (it is his number, not his words), and the coaching panel no longer picks one as its Chris quote. **6b (extract v4 / `v2-rag-3`):** two three-state comparisons replace `comparison_to_intended_outcome.made` (v3 fields still written; stored v3 lifted on read); `equipment.states_effect_on_observed_performance`; `delivery_to_peer` Mark-only + `speakerGuard` in code; scale: one complete instance alone = 3 at most; `exemplar_anchor`; `gap_to_next` quotes Mark only; `unitCheck` / `mergePassage` follow (a rewrite that adds the ideal no longer erases a comparison to intent made elsewhere); template 2,989 tok of 3,000 — wording trimmed, no standing rule removed. · Verified offline: `npm run test:ingest` (exemplar text; three fit the slot even from an oversized extraction; re-run writes nothing and makes no model call; the pinned AI score survives rescore + re-ingest; Gates's card supersedes nothing; comment split is verbatim and idempotent; **agreement reproduces §16: blind 3/6 exact · 5/6 within one · Evaluate +2; all 5/12 (41.7%) · 11/12 (91.7%)**; scoring 7uk7lnh is shown 7n6ry6d's exemplar and not its own or Gates's); `test:score` (v4 facts, speaker guard on the 7uk7lnh peer-restatement case, exemplar bookkeeping); `test:history`, `test:exam`, `test:zoom`; `vite build` clean. · **Not verified here (no keys): every live step.** Done-when is open on all three counts until Mark runs it: (1) `npm run ingest:scores` → two exemplars in the store; (2) `npm run score -- 7uk7lnh --compare chris` and `-- 7n6ry6d --compare chris` → each shows the other's exemplar, cites it, within-one on all six; (3) **the third blind scorecard does not exist yet** — it arrives when Chris scores a fresh exam in the app (the same event that closes session 5), and the submit hook ingests it. · Decided: six new §15 rows. · Flag: with n = 2 and self-exclusion, each rescore sees exactly one exemplar, and 7n6ry6d's scorecard is the one `v2-rag-2`'s AT-bar paragraph was written from — agreement on these two after 6b is a regression check, not evidence; the first evidence is the third session, scored blind by `v2-rag-3` before Chris sees it. · Deferred: see §17.
- **2026-09-19 · Session 5 — live check, left open** · Deployed. Coaching panel made collapsible like the other sections (choice remembered). Try-that-line-again on `ma_7uk7lnh`: 139 s first try (≈90 s of it rebuilding the shed extraction, once per session per browser), **50 s steady state** — the under-a-minute half of done-when is met, with ~10 s of margin; not tuned on two data points (lever in §17). Chris's 9/18 verbal scorecard posted on 7uk7lnh in the parsed format (`scored blind`); the tab shows exact 3/6 · within-one 5/6 · same result, Evaluate AI +2 — matches the 9/18 log. · **Open: Chris scores one session blind in the new app** (he is away ~1 week). Decided with Mark: move on to session 6 and close this item when Chris is back; preferably on a fresh exam scored by `v2-rag-2`, which also gives session 6 its third scorecard. Do not rescore 7uk7lnh — it is the only session scored before Chris saw anything.
- **2026-09-19 · Session 5 (frontend — MA History + coaching)** · Mark: happy with the MA History layout; the problem is that scores have not been consistent across the application. Diagnosis (old JSX): `summary` read in eight places — exam screen ranks attempts by sum of the legacy six, MA History shows top-level scores, Progress trends `if (scores[k])` (drops 0/missing, so averages run over different lines per session), prompts read legacy keys by name, and three different fallbacks when the JSON doesn't parse; session 3's `sectionAverages` repeated it (a legacy attempt got "TU 4.00" from one line). · Built: `src/lib/scorecard.js` — `scorecard(session,{viewer})` is the only reader: form (`2026` | `legacy`), six lines with null for missing, section averages and verdict computed from the lines (stored values ignored, mismatch → `warnings`), best-attempt rank (MA avg + TU avg; legacy never outranks 2026), mentor scorecards (structured blind items and hand-posted "… scorecard …" lines, attributed to the mentor named), per-line deltas, and the blind seal; `exam.js`, `ATExam.jsx` score screen and the legacy prompt history now go through it (no score arithmetic left elsewhere). `components/ScoreViews.jsx` (chips/grid/verdict/old-scorer tag — same view in the exam and in history). `tabs/MAHistory.jsx` (old layout; newest session open, others collapse; structured sections rendered in full with speaker colors — fixes Chris's "I only got one sentence" of peer delivery; rescore through extract → evaluate with step line, previous score kept in `score_history`, **no legacy fallback on rescore** — a failure leaves the existing score alone; draft-persisting comment box with mic; delete and rescore are candidate-only). `tabs/Rationale.jsx` (§12.2: Chris quoted inline with date, ingest-written title shown outside the quotation marks; PSIA/form citations and the level-by-level ladder collapsed). `tabs/BlindScoreForm.jsx` (§12.1: six tap rows on the 2026 form, live section averages, note, draft in localStorage, confirm, final on submit). Blind: `sealSessions()` takes the AI summary out of the session object before it reaches React state for any session the mentor hasn't scored; a sealed card carries no numbers or text; other mentors' scorecards are withheld from the thread until he submits his own; on submit the item stores `scores`, `note`, `ai_at_submit` and a text line in the same format Mark posted by hand for 7n6ry6d. **Coaching:** `lib/coaching.js` + `tabs/CoachingPanel.jsx` — per line, lowest first: what Mark said (located in the transcript, typo-tolerant), the evaluator's rewrite, the because-clause, the first Chris citation; labelled AI SUGGESTIONS; says so when the quoted line was the *peer's* (7uk7lnh Prescription), came only under examiner probing (7uk7lnh Desired Performances), or was in private notes and never spoken. **Try that line again:** `extractPassage` (same clerk, same schema, peer intent + task as reference) → `mergePassage` (items quoted from the replaced sentence out, passage items in, stats recomputed) → `assemble(original, {evidence: merged})` → `buildEvaluateLineUser` (one line, full ladder, not told the previous score) → `normalizeLine` (same clamps, citation check, equipment guard) + `unitCheck` (the evaluator's unit, checked in code against the passage alone, with the missing links named). Practice log in localStorage, last 5 per line. **Pre-exam brief** on the setup screen: three lowest lines of the last 2026-form session, ranked by Chris's scorecard when he has one. `parseAIJson` strategy 2 now rebuilds rationale for `desired_performances` and `equipment`. Summary fit: cited text 400 (Chris) / 200 (others); shed order is now justifications → extraction → citation_details, because coaching needs the extraction (7uk7lnh's was shed). `src/api.js` += `saveMaSummary`, `appendFeedback` (re-read + merge), `deleteMaSession`, `scoreLineTry`. · Verified: `npm run test:history -- <export>` (offline; also runs every row of the live MASessions export through `scorecard()`: 13 sessions → 1 on the 2026 form, 10 old-scorer, 2 unscored, 0 unparsed, 0 warnings; reproduces the 9/18 comparison exactly — exact 3/6, within-one 5/6, Evaluate +2, same result); `scripts/rendertest-history.jsx` server-renders the tab from the live export as Mark, as Chris before scoring (no AI output in any sealed session), and as Chris after submitting (reveal, per-line delta, coaching read-only); `scoreLine` run end to end with models and store mocked (query = original extraction, evidence = merged, nothing written); `vite build` clean; score/exam/zoom selftests pass. · **Not verified here (no keys, no deploy): the two model calls of try-again and its wall-clock; the blind submit against the live Sheet.** Done-when stays open until deployed: Chris scores one session blind; Mark reworks one line in under a minute. · Deferred: see §17.
- **2026-09-19 · Session 2 (backend)** · Built: `lib/prompts/zoom.js` (`zoom-1`: mentor-only, one point per item, no generalizing, read-aloud ≠ asserted, modeled deliveries kept whole as `exemplar`, `hedged` + note, ASR fixes listed per chunk, verbatim `quote`, `tool_feedback` side channel); `lib/ingest/zoom.js` (Plaud/Zoom parser, speaker report, ~12K-char core windows with context-only padding run as parallel Opus calls, quote-in-transcript ≥60% and text-overlap ≥40% guards, dedupe, §17 `watch` marks, 409 on re-ingest unless `replacePending`); `store.js` += `insertPending` (no embedding), `listPending`, `liveStatements`, `supersessionCandidates`, `canSupersede`, `editPending`, `approveChunk` (edit → embed → approve in one update → supersede), `deletePending`, `countBySourceRef`, `deletePendingBySourceRef`; `api/ingest/zoom.js`, `api/chunks/pending.js` (GET/POST, trimmed shape, candidates per chunk), `api/chunks/approve.js` (approve / edit / delete, bulk ids); `scripts/zoom.mjs`, `scripts/selftest-zoom.mjs` (offline, passing; also run against the real 9/18 transcript: 240 turns → 3 windows of ~4.5K tok); `vercel.json` `api/ingest/*.js` 300s; `lib/http.js` `preflight` takes allowed methods. · Decided: embedding at approval, not at insert; speaker map is a prior and chunks under a non-mentor label are flagged for Chris rather than dropped (the 9/18 labels flip mid-turn: Chris appears under all three); tool feedback is not stored; approved chunks immutable; delete is a hard delete of pending only. 9/18 map from reading the transcript: Speaker 1 = chris, Speaker 2 = mark, Speaker 3 = unknown (mixed). · Deferred: see §17. · After the first live runs — `zoom-2` (standalone rule, coverage rule with ski vocabulary for ASR repair, tool feedback exclusive, progress/gear remarks excluded, fixed scores format); session-range tagging (`metadata.session_id`); two-pass union; score-line guard; `exclude` ranges. · **Done-when met 2026-09-19:** 9/18 Plaud transcript (240 turns, 1:06) ingested through the deployed endpoint — speaker map 1 = chris, 2 = mark, 3 = chris (Mark), `ma_7uk7lnh` tagged for 00:00–24:30 and 33:30–35:30 (the large-radius notes at 25:41 are a separate doc, not an MA History row — untagged), 51:24→end excluded (ski choice, per Mark). Two passes 36 + 34 → **37 pending** under `zoom:2026-09-18` (13 correction · 10 principle · 7 exemplar · 4 physics · 3 mental_model; 2 hedged, 3 check-speaker, 28 with supersession candidates), all listed by `/api/chunks/pending` with context, tags and verbatim quote; one item rejected by the score guard ("Describe 2"); stored scores chunk reads CE 2 · Ev 2 · Rx 3, matching the log. Default search returns 0 `chris_zoom`; `includeUnapproved` also 0 (no embedding). 190 s wall-clock.
- **2026-09-19 · Decision** · Coaching is a first-class feature. Mark: the per-line rewrites were "one addition I like that I didn't plan for." Session 5 scope extended (coaching panel, try-that-line-again, pre-exam brief); recurring gaps added to session 8. Rewrites are AI suggestions and are hidden from Chris until his blind score is in.
- **2026-09-19 · Calibration data (no build)** · First truly blind comparison. `ma_7uk7lnh` (Fall Line Bumps, scored by `v2-rag-2` before Chris saw anything): AI 3/4/3 · 2/3/4 vs Chris 2/2/3 · 2/3/3 (given verbally on the 9/18 call; Bio and Equip relayed by Mark). Exact 3/6, within-one 5/6; both agree Does Not Meet. Miss: **Evaluate +2**. Running total incl. 7n6ry6d (not blind — the scorer was calibrated on it): exact 5/12 (42%), within-one 11/12 (92%) against targets of 60% / 85%. · Finding: both over-scores (Evaluate 4, Equipment 4) have `evidence_count` "complete: 1 · partial: 0" and a rationale that uses "single instance / concentrated in one moment" only to deny a 5. The template's scale says 2 = "a single complete one among partials" and 4 = "complete instances are the norm"; one complete instance with no partials falls in the gap and the evaluator resolved it upward. See §17. · Equipment dispute (Mark: I did say how it affects performance; Chris: it was a recommendation): the unprompted statement was a recommendation plus the predicted benefit of the change; the *effect* statement was in private notes (never spoken) and in a prompted Q&A answer that stops at a body state. Chris's 3 stands as ground truth and is consistent with the form wording ("identifies effects of equipment on skier's performance"). · The 9/18 Plaud transcript is session 2's input; speaker labels in it are unreliable (Speaker 1 is mostly Chris, not Mark; a spurious Speaker 3; one stray Plaud AI message) — attribution must be by content with Mark's confirmation.
- **2026-09-18 · Session 3 (frontend) — finish** · Session 3's code (Vite shell, login, tab nav, `tabs/ATExam.jsx` 7 phases, mic/speaker, localStorage persistence, hash-diff save, peer/examiner prompts rewritten around Mark's "examiner asks what I already said" complaint) landed in commit `ee5cff6` without a log line. Finished here. Mark's complaints: the tab opened on his last, already-saved exam with no visible way to start another; "Present" should read "Examiner – Present". · Built: always-visible **＋ New exam** button (confirms only when there is unsaved work) and a real "Start a new exam" button on the score screen; `loadExam` no longer restores an exam that is scored **and** saved (in-progress and scored-but-unsaved still restore); phase labels "Examiner – Present" / "Examiner – Q&A". **Scorer swap:** `/api/score` split into `/api/score/extract` + `/api/score/evaluate` (`lib/http.js` shared; one-shot `/api/score` kept for the CLI); tab calls the two in sequence with a step line; on failure falls back to the old `SCORER_SYSTEM` and says so on screen (`scorer: "legacy"`, never passes as a 2026 score, never wins best-attempt over a new-scorer attempt). **Score panel on the 2026 form:** MA / TU groups, section averages, Meets / Does Not Meet, Describe + Communication as diagnostics, per-line rationale + evidence count + gap-to-next + "Based on: Chris, 2026-09-17 — …" (hover for text; full §12.2 UI is session 5). Best attempt ranks on MA avg + TU avg. **Prompts on the 2026 form:** `AT_STANDARD` rewritten to the six lines; examiner treats never-addressed Equipment / Desired Performance as genuine gaps, asks for the *how* on asserted links and for chains that stop at a body state; peer loses the thread on an over-long prescription instead of summarizing it neatly. **Saved summary** = full score result minus `debug` and the retrieval manifest, cited text cut to 400 chars, shedding guard at 45K (real 7n6ry6d result: 33.8K); `allAttempts` gains `section_averages`, `scorer`. `scripts/selftest-exam.mjs` (offline; persistence rules, summary size, old-app `hasFullScores` round trip). · After Mark's first look: **Sheet loading** — `src/api.js` now retries the transient Apps Script failures (HTML/404, "Unknown action") on reads *and* writes (4 tries, backoff); a transient failure on `update` previously fell through to `create` and could duplicate a row; real answers ("Row not found") are not retried. `sheetHealth` distinguishes empty from failed; the tab no longer blocks on the Sheet load (history only feeds the examiner's probes); failure banner has a Retry button. **＋ New exam** shows only once there is work in the exam (`hasContent`), not on a blank form or the score screen (which has its own button). · Decided: revisions stay at 3. · **Done-when met 2026-09-18:** Mark ran a full AT Exam in the new app (`ma_7uk7lnh`, Fall Line Bumps); row in `MASessions` has all six sections and a 34.5K summary scored by `v2-rag-2` through the split endpoints — six 2026 lines (3/4/3 · 2/3/4), MA 3.33 · TU 3.0, Does Not Meet, 22 valid citations, no ladder skips, no guards, no legacy fallback. First session scored blind-ready: Chris has not seen these numbers.
- **2026-09-17 · Session 4 (backend)** · Built: `lib/vocab.js`; `lib/prompts/extract.js` v2 (`connections[]` with per-link nulls and a never-repair-his-reasoning rule, `task`, `desired_performance`, `equipment.addressed`, `prescription.chain`, `comparison_to_intended_outcome`, code-computed `delivery_stats`; `extractionToText` substance-first); `lib/assembler.js` (§7 slots, PSIA 12K cap, self-exclusion, manifest); `lib/prompts/evaluate.js` (six 2026 lines, case-for-5/4/3 then 2/1, low- and high-guards, legacy diagnostics, 1.9K tok); `lib/score.js` (normalization: code-computed section averages/meets_standards, citation validation + `citation_details`, equipment guard); `api/score.js` (`sessionId` or inline `session`, read-only, `maxDuration` 300); `scripts/score.mjs`; `scripts/session4-migrate.mjs` (retag 2 form chunks; Chris's 9/17 comment split verbatim into 7 single-point chunks, whole-comment chunk superseded); `scripts/selftest-score.mjs` (offline, passing). `store.js` CRITERIA now includes `desired_performances`, `equipment` (were being silently dropped). · Decided: see four new §15 rows; done-when amended in §13. · Deferred: see §17. · **Live run 1 (v2-rag-1): FAIL.** 7n6ry6d scored 4/4/4 · 4/3/1 vs Chris 2/2/2 · 2/2/1 (four lines +2; Equipment and the overall result matched). Ben 4/4/4 · 4/4/1, Chuck 3/4/4 · 4/3/1. Diagnosis from `out/score-7n6ry6d.json`: both steps generous. Extract marked "X was caused by Y" as `how_stated`, put body consequences in the outcome slot, took the predicted benefit of the fix as the task's desired performance, and called a description of the observed turn a comparison to intent. Evaluate wrote "3: No case needed — the 4 case holds" on every line (started at 4, confirmed it), called a connection with null ski performance and outcome "complete", and related Mark's observation to the peer's intent itself. · **Fix (extract v3 / evaluate v2-rag-2):** each generous field now defined by what it is not; no stitching across sections; `cause_effect_chain` rendered in code from `connections[]`; code-computed `connection_stats` and `connections[].complete`; evaluator gets a defined unit of evidence per line, a scale that counts complete units, a bottom-up ladder with every rung written (`quality.ladder_skipped` flags violations), and an explicit AT bar (L3-grade analysis is the starting point, not a 3/4 — sourced from Chris's scorecard, overridden by exemplars when they exist). Template slot 2K → 3K. `lib/sheet.js` retries 4 → 6. · Also: Anthropic client timeout 240s / 1 retry (SDK default 10 min × 2 retries hung a run for 30 min) and per-step progress lines in the CLI. · **Live run 2 (extract v3 / v2-rag-2): PASS.** 7n6ry6d 3/1/3 · 1/2/1 vs Chris 2/2/2 · 2/2/1 — max |Δ| 1, two exact (Biomechanics, Equipment), result matches (Does Not Meet). Ben 3/3/2 · 2/3/1, Chuck 2/2/3 · 3/2/1; Ben above Chuck on Cause/Effect and Biomechanics in line with evidence counts (ski-perf/outcome links 5/4 of 8 vs 1/2 of 6), so the AT bar did not flatten real differences. No ladder skips, no invalid citations, no guards fired. Done-when met. · Caveat: calibrated against one scorecard; within-one 6/6, exact 2/6 on n=1 says nothing yet about the §1 targets.
- **2026-09-17 · Decision** · Chris confirmed the 2026 six-criterion form is the current standard. §5.3, §8.3, §15 updated; follow-through listed in §17. Template-version form chunks (`at-ma-tu-assessment-form.md`, 12) to be superseded by the 2026 version (`at-ma-tu-assessment-form-2026.md`).
- **2026-09-17 · Session 1 addendum 2 — first real Chris input** · Chris scored session `ma_7n6ry6d` on the official form and commented in the tracker himself; comment ingested as `chris_comment` (`author: chris`, store now 3). His scores posted by Mark as a structured comment line for session 6 to parse. · Finding: the form Chris used is a **2026 revision with six MA/TU criteria** — MA: Cause and Effect, Evaluate, Prescription; TU: Understanding of Desired Performances, Understanding of Biomechanics/Physics, Equipment. Describe and Communication are no longer scored lines; revised learning outcomes. Ingested as `reference/at-ma-tu-assessment-form-2026.md` (9 chunks, deterministic). The 11-criterion template version remains live until Chris confirms which form is current; then supersede the other. · His comments define AT-level MA in his words (X→Y→Z frame tied to the task; peer piece = "coaching cue plus", a few sentences; equipment–biomechanics–desired-performance trio; "the how in depth"). Session 4's evaluate prompt should be built around them. · Fixes: `lib/sheet.js` now retries transient Apps Script "Unknown action" failures.
- **2026-09-11 · Session 1 addendum** · Official AT MA/TU Assessment Form obtained and ingested as `reference/at-ma-tu-assessment-form.md` — 12 `psia_doc` chunks (scale + section rule, one per criterion with section Learning Outcome), parsed deterministically, no model call. The 12 summarized scorecard chunks from `psia.md` marked `superseded_by` the form's scale chunk via new `scripts/supersede.mjs`. · Incident: first ingest used the generic Sonnet prose chunker with a guidance hint listing criterion names; Sonnet produced 36 chunks, most manufactured from the hint, including an invented 1–4 rating scale. All 36 hard-deleted (`scripts/cleanup-form.mjs`), old chunks restored, re-ingested cleanly. · Fixes: (a) `groundedness()` guard in `lib/ingest/psia.js` — every Sonnet-produced chunk is rejected unless ≥60% of its sentences appear in the source text; (b) chunker prompt now states guidance is never source; (c) small regular documents get deterministic parsers, not the model. · Note: the summarized scorecard already listed all eleven criteria — the six-criteria scorer was an original app design choice, not a reference gap. Decision on expanding the taxonomy logged in §17.
- **2026-09-10 · Session 1 (backend)** · Built: `supabase/schema.sql` (§5.1 + `match_chunks` RPC with §7.3 hard filters, `text_hash` unique index for idempotent ingest), `scripts/migrate.mjs`, `lib/embed.js` (Voyage `voyage-3`, 1024-d, locked), `lib/store.js`, `lib/llm.js`, `lib/parseSummary.js` (harvested from old JSX), `lib/sheet.js` (harvested column guards incl. `b`-for-`id` header), `lib/prompts/extract.js` (§8.1), `lib/ingest/psia.js` (§6.1 chunker), `scripts/bootstrap.mjs`, `scripts/search.mjs`, `api/chunks/search.js`; `reference/` with five source files. · Decided: (a) reference material is ingested from `/reference/*.md` checked into the repo, not from `Config._REFERENCE_MATERIALS` — that Sheet row is empty and the ~47K of PSIA text was a hardcoded default in the JSX; (b) `extract.js` built in session 1 (spec said session 4) because `past_session` ingestion needs it; session 4 refines; (c) original PDFs replace the summarized IDP section (excluded from `psia.md`; full IDP = 44 task chunks, one per activity); (d) Performance Guide L1–3 criteria ingested with `l3_vs_at` on L3 chunks as the baseline AT is compared against; (e) `private_notes`/`root_cause` exam sections fed to Step 1 as observation-only, never as communication. · Deferred: AT MA/TU Assessment Form (see §17); done-when verified 2026-09-11: store 157/1/2/6 (psia_doc/mentor_assessment/chris_comment/past_session; `ma-sample-2/3` seed sessions superseded via `EXCLUDE_SESSIONS`); retrieval checks pass; `POST /api/chunks/search` live on the new production site. · Environment: new repo `markanderson71/RM-AT-Developer`, new Vercel project `rm-at-developer` (https://rm-at-developer.vercel.app); old site left untouched, same Sheet. `past_session` embedded text is the extraction only — scores were moved out of the text after the first retrieval check showed they compressed all sessions into one similarity band.

## 17. Deferred / open

- **Taxonomy change follow-through** (decision in §15, 2026-09-17): ~~session 4 evaluate template and `justifications`/`gap_to_next` keyed on the six~~ (done); session 5 MA History shows the six + section averages + Meets/Does Not Meet; session 6 Chris's scoring form is the 2026 form layout (incl. Needs/Safety, Behavior Management as optional); Sheet `summary` JSON gets `section_averages` and `meets_standards`. ~~`EXTRACT_SYSTEM` in `lib/prompts/extract.js` should add explicit inventory items~~ (done, extract v2): `equipment: { addressed: bool, quote, linked_to_biomechanics: bool, linked_to_desired_performance: bool }` and `desired_performance: { stated: bool, quote, fundamentals_blended: [...] }` — Chris's 9/17 Equipment score of 1 meant "not addressed at all"; the evaluator must see absence explicitly, not infer it. Chris's trio (equipment × biomechanics × desired performance, with a worked causal example) is the target shape for that line. Re-tag existing chunks: nothing required — tags are additive; `equipment` skill tag already exists.
- ~~**MASessions header cell A1 reads `b` instead of `id`**~~ (closed by 7b: the migration folds `b` into `id`; the store never holds `b`. `src/api.js` still sends `b` alongside `id` — harmless, remove with the next MA History touch.)
- **After 7b:** the Sheet is a snapshot; delete it and unpublish the Apps Script after session 10 ships. Legacy Config rows (`_REFERENCE_MATERIALS` empty, `_MA_SESSIONS`, `cp_*`, `vid_*`, `cli_*`) were copied as-is — sessions 9–10 decide what stays. `old-app/api/sheet.js` must be regenerated (`node scripts/build-old-app-sheet.mjs`) whenever `lib/ops.js` changes, until the old app is retired. `notify` needs Chris's real address in `_MENTOR_PROFILES` (still Mark's Gmail from the May test) and a verified Resend sender for a non-test domain. Postgres row-level security is on with no policies — only the service key reads it; if the browser ever talks to Supabase directly (it shouldn't), that changes.
- ~~**`/api/chunks/search` returns full `metadata`**~~ (session 8: `trim: true` drops it; the review UI uses `chunks/pending`, which was already trimmed).
- **ivfflat index built on an empty table** (`lists = 50`). If recall looks off after bootstrap, `reindex index chunks_embedding_idx`.
- Fine-tuning: revisit with 50+ blind scores if agreement plateaus below target.
- Retiring in-prompt reference material from the old `buildScorerPrompt`: after session 3 proves retrieval; keep old path as fallback until then.
- ~~Progress tab trend analysis redesign to use exemplars and agreement data.~~ (session 8: trends are the six lines through `scorecard()`, Chris's card beside; agreement is its own panel.)
- Gates and Mike as blind scorers: same form, `author` tag differs; inter-rater data between mentors is a bonus.
- **Coaching from `gap_to_next`** — promoted to scope 2026-09-19: panel, try-again, and pre-exam brief are in §13 row 5; recurring gaps in row 8. Open design point for session 5: "try that line again" needs a single-line evaluate path (`/api/score/evaluate` with a `lines: [key]` filter and a shortened ladder) — a new code path, to be specced in that session's plan. Route rewrites to Chris: his reactions are correction chunks (§6.1).
- ~~**Single-instance gap in the evaluate scale (two scorecards now).**~~ (done, `v2-rag-3`, session 6 — live check pending) One complete instance and nothing else scored 4 twice on `ma_7uk7lnh` (Evaluate: Chris 2; Equipment: Chris 3). Proposed wording: one complete instance = 3 at most ("appears, but not with consistency"); 4 requires the element to recur or be threaded through the analysis; for Equipment and Desired Performances, a recommendation or the predicted benefit of a change is not an instance — the instance is the stated effect on *this skier's observed* performance. Apply together with exemplar ingestion in session 6 so the change is tested against both Chris scorecards at once, not tuned line by line.
- **Evaluate is unstable, not biased** (still open; Chris's definition now extracted — see the Evaluate comparison item above): −1 on 7n6ry6d, +2 on 7uk7lnh, both driven by the single boolean `comparison_to_intended_outcome.made`. Chris's verbal reasoning on the 9/18 call (session 2 will extract it) should define what he counts as the comparison — likely the task's ideal, not only the peer's stated intent.
- **Sparring peer said "pivot" as an intent and Chris objected on the call** — a bug report on `PEER_SYSTEM` (the peer's vocabulary should be what a certified instructor would actually say). Pick up with session 2's extraction of that passage.
- **Binary extraction flags are too sharp at the bottom.** On 7n6ry6d, `comparison_to_intended_outcome.made = false` and `desired_performance.stated = false` drove Evaluate and Desired Performances to 1 where Chris gave 2 ("beginning to appear"). Within-one, but the direction is informative: Chris gives partial credit where the clerk's boolean says absent. Candidate fix: a three-state field (absent / partial / made) with the partial evidence quoted. Do not tune until a second Chris scorecard confirms the pattern.
- **`connections[].complete` never fired** (0 on all three sessions; `how_stated` true on 1 of 21 connections). The evaluator discriminated on the partial counts, so scores were sane, but the flag carries no information yet. Revisit where Chris's line for "the how" sits once he has scored a session he considers to have one.
- **7n6ry6d and Ben both scored Cause/Effect 3** despite Ben's clearly stronger evidence. Coarse at the 2/3 boundary; exemplars should resolve it.
- ~~**Session 6:** 7n6ry6d scorecard is exemplar #1; `/api/ingest/comment.js` splits long comments by paragraph~~ (done). **Still open:** after a score is saved, write/refresh the `past_session` chunk (§6.7 "continuous"). Not built: `past_session` chunks feed nothing in scoring (never shown as examples), and it would be a 13th serverless function. If wanted, fold it into `/api/ingest/score` as `{ kind: 'past_session' }`.
- **Session 5:** `parseAIJson` strategy 2 (regex fallback) rebuilds `score_rationale` for the legacy six keys only; extend to the 2026 keys when the tab is built. Sheet `summary` should store the full `/api/score` result minus `debug`.
- **Existing `past_session` chunks are extraction v1** (exemplars are built from a fresh v3/v4 extraction — `ingest:scores --reextract` after deploying 6b puts both on v4; the remaining `past_session` chunks stay v1). Not used by the evaluator, so no re-bootstrap required; re-extract when session 6 builds exemplars so they carry `connections[]` and `equipment`.
- **Chris–Chris conflict detection** (§5.6 last rule) is done by the evaluator, which sees dates and reports `chris_conflicts`; the assembler can't detect contradiction without a model call. If conflicts start appearing, add a review list to session 8's approval view.
- **Chris slot minimum of 5** (§7.1) is met only when the scored session isn't 7n6ry6d (7 split chunks + 2 comments + assessment). `meta.context.chris_under_min` reports it. Zoom chunks fix this.

- **Evaluate comparison — Chris's definition is now in the store (pending):** 49:35 "provide a comparison of how they were relative to their desired outcomes, and then how does that desired outcome or their intent match to the ideal" and 42:23 "quality and accuracy… go to the component parts… compare that relative to an ideal… always in relationships between fundamentals, phase to phase." Two comparisons, not one: performance vs the peer's intent, **and** intent vs the task's ideal. ~~Session 6: replace the boolean with `{ vs_intent, vs_ideal }` (three-state each)~~ (done, extract v4; live test against both scorecards pending). When Chris approves the 49:35 / 42:23 chunks, check the template's structural wording of the Evaluate unit against his; his wins.
- **`PEER_SYSTEM` bug confirmed by Chris (44:09):** the AI peer's answers are not what candidates would say ("pivot" as an intent; long tidy answers). Chris, 45:00: a real peer gives enough but not so much that it sinks them — "you might be more brief"; it is on the candidate to ask one targeted question. Fix with the Sparring tab's next touch (session 11 at the latest). Also from `tool_feedback`: the summary Chris saw lacked the peer-delivery text ("I only got one sentence") — check what MA History shows a mentor in session 5.
- **Zoom ingest ran 190 s of a 300 s limit** (6 parallel Opus calls for a 66-min call). A 90-min call may not fit: make `passes` sequential-with-budget, or split ingest into a per-window endpoint driven by the CLI.
- ~~**For Chris at approval (session 8):** … show `quote` next to the cleaned text~~ (done: **Heard:** under every statement, bracket warning, ASR fixes). The 37 pending chunks from 9/18 are still pending — Chris's first pass is the live test of the view and of `zoom-2`.
- **Mutating endpoints have no real auth** (after 7b this includes every write to `ops_rows` through `/api/sheet`, same trust model as the Apps Script had — anyone with the URL could write then too). ~~`/api/chunks/approve` trusts a client-asserted `actor`~~ **Session 8: `chunks/approve`, `chunks/pending` and `agreement` require the shared `MENTOR_TOKEN`** (open until it is set — Mark sets it on Vercel and hands Chris the code). Still open: the token is shared, so `actor` and `mentor=` are still client-asserted among token holders; `/api/ingest/zoom` and `/api/sheet` writes are still open; a per-user signed login is the real fix.
- **Supersession candidates are tag-ranked, not semantic** — pending chunks have no embedding by design. If Chris finds the candidates unhelpful in session 8, embed the pending text as a *query* at list time (never stored) and rank the pool by similarity.
- **Deleted pending chunks leave no trace.** What Chris deletes is the best signal for tuning `zoom-1`; if the delete rate is high after two or three calls, log `{text, reason}` to a side table before deleting.
- **Zoom extraction quality is unmeasured.** After Chris's first approval pass (session 8, or by CLI before): approve / edit / delete rates per type, and what he says is missing. Tune the prompt on that, not on our reading of the output.
- **Chris's ski-choice advice on the 9/18 call (57:37–58:56) is excluded by rule** (advice about Mark's own skiing, not MA/TU assessment). It is a worked equipment × performance argument; if Chris wants it in, the rule gets an exception.

- **Rule for every later tab (sessions 7–10):** no component reads `summary.scores`, computes an average, or picks an attempt. Call `scorecard(session, { viewer })`. Progress trends (session 8) plot 2026-form cards only; old-scorer sessions appear as a labelled, separate series or not at all.
- **Post 7uk7lnh's verbal scorecard so it counts.** Chris's 9/18 scores exist only in §16. One comment on that session in the parsed format makes it a mentor scorecard everywhere: `Chris scorecard 2026-09-18 (2026 form, scored blind), posted by Mark from the 9/18 call: MA: cause_effect 2, evaluate 2, prescription 3 | TU: desired_performances 2, biomechanics 3, equipment 3`. ("scored blind" in the text sets `blind: true`; without it a hand-posted card is marked not blind, as 7n6ry6d is.)
- **Session 6 inputs from session 5:** mentor scorecards are `mentorFeedback` items with `kind: "blind_score"`, `scores`, `note`, `blind`, `ai_at_submit { form, scores, scorer, scored_at }`; hand-posted lines parse with `parseScoreLine`. Agreement must compare the mentor's scores with `ai_at_submit`, not with the current summary (rescoring changes it). `mentorScores()` in `scorecard.js` is the reader — move it to `/lib` if the API needs it.
- **The blind seal guards against anchoring, not inspection.** The summary is kept out of React state for an unscored mentor, but it is still in the `/api/sheet` response. A real server-side gate needs a mentor-scoped read endpoint; pair it with the auth item above (a 13th function — see the function-count item). Session 8 kept the rule client-side: the Progress tab never asks `/api/agreement` for anyone but the viewer, and trends / gaps / AI analysis are built from `scorecard(session,{viewer})`.
- **Try-again timing is unmeasured.** Budget: passage extract (Sonnet, ≤2.5K out) + one-line evaluate (Opus, ≤2K out) + assemble. If the first live runs exceed a minute, the lever is the evaluate output (ask for rungs 2–5 in one sentence each), not the ladder itself. When a session's extraction was shed (7uk7lnh), the first try re-extracts the whole session (~1 min, cached in localStorage afterwards); a rescore stores the extraction and removes that cost.
- **Try-again results are not routed to Chris** (§17 "his reactions are correction chunks"). Natural home: a "send this rewrite to Chris" button that posts the before/after to the thread. Not built — outside row 5.
- **Evaluator quoted the peer as Mark** in 7uk7lnh's Prescription `gap_to_next` ("Instead of 'Focus on initiating with a guided ski…'" is the peer's restatement). ~~The panel flags it; the fix belongs in `extract`~~ (done in both: extract v4 rule + `speakerGuard` in code; evaluate rule for `gap_to_next`). Original note: the fix belongs in `extract` (`prescription.delivery_to_peer` should be Mark's words only) or in the evaluate rule for gap_to_next. Pick up with session 6's template change.
- **`mergePassage` is heuristic** about which inventory items the replaced sentence produced (≥70% token overlap with the item's quote). Wrong in the conservative direction — an item that should have been removed stays, so practice scores err low, not high.

- **Session 8 follow-ups:** (a) `challenge` reaches the examiner (`mentorGapsBlock`) and the store (bootstrap) — not yet the pre-exam brief on the AT Exam setup screen or the Journal Challenge prompt beyond what `mentorGapsBlock` already carries; add with the next touch of those tabs. (b) `mentor_assessment` chunks are still written only by `bootstrap.mjs` (§6.5 "on save" not wired) — a saved assessment reaches the scorer at the next bootstrap; fold into `/api/ingest/comment` as `{ kind: 'assessment', mentor }` (no new function). (c) `PRIORITY` in `progress.js` stars the 42:23 / 49:35 statements by timestamp — remove once approved. (d) The approval tally lives in the browser's localStorage; if the delete rate matters for tuning `zoom-2`, log `{text, reason}` server-side (the earlier §17 item). (e) Agreement's "AI leans" uses ±0.3 mean Δ as the threshold — arbitrary at n=1–2; revisit at ten cards. (f) The recurring-gap theme detection is lexical (shared content words in the because-clauses); it will call two different gaps "the same" when the template phrasing repeats. Good enough to point at a line; not evidence.

## Appendix A — Glossary for the builder

- **MA** — Movement Analysis. Observe a skier, describe, diagnose cause, prescribe a change, explain why.
- **AT** — Alpine Trainer. PSIA certification above Level 3. AT candidates train instructors, not public.
- **Fitts & Posner scale** (as used in PSIA-RM): 1 not observed · 2 beginning to appear · 3 appears inconsistently · 4 appears regularly (pass) · 5 frequently, above required · 6 continuously, superior.
- **Fundamentals** — pressure, edging, rotary, plus balance/fore-aft. Skills Concept is their blend.
- **DIRT** — duration, intensity, rate, timing of a movement.
- **IDP** — Instructor Development Pathway tasks (e.g., White Pass turn, outside-ski turn, railroad tracks). Each has defined ski and body performance.
- **Two-audience** — in the AT MA Exam, the candidate delivers to a peer (what/how, their language) and separately to the examiner (technical why). Explaining physics to the peer is coaching, not MA.
- **Prompted vs unprompted** — whether the candidate showed a concept in the presentation or only after examiner probing. Refinement under probing is additive; creation under probing caps at 4.
- **Sidecut → reverse camber → groove → steering** — the physics chain Chris keeps pushing Mark to complete unprompted.

- **Vercel function count is 12** (`chunks/approve`, `chunks/pending`, `chunks/search`, `claude`, `sheet`, `score`, `score/extract`, `score/evaluate`, `ingest/zoom`, `ingest/score`, `ingest/comment`, `agreement`) — the Hobby plan's per-deployment ceiling. §10 still lists `/api/ingest/psia.js` (never built; PSIA ingest is CLI-only) and sessions 7–8 will want a mentor-scoped read endpoint. Before adding a 13th: fold `chunks/*` into one handler with an `action`, or `ingest/*` likewise — or confirm the plan is Pro.
- **`desired_performance.stated` is still a boolean.** The three-state change was made for the comparison only (§17 said not to tune DP until a second scorecard confirmed the pattern; on 7uk7lnh DP was exact at 2). Revisit with the third scorecard.
- **Exemplar ranking is untested at n > 3.** With two or three exemplars every eligible one is taken; same-MA-type-then-similarity ordering starts to matter around ten. Add exemplar labels to the §14 retrieval eval when there are enough to choose between.
- ~~**Agreement endpoint returns AI numbers and has no auth**~~ (session 8: `MENTOR_TOKEN`; the tab requests `mentor=<viewer>` only for a mentor).
- **Blind-form notes are one field.** §6.4 imagined a note per criterion; the form has one. Notes are split by paragraph and keyword-tagged to lines. If Chris's notes turn out to be line-specific, six optional note fields on the form are the cleaner source (session 8 touch).
- **Comment `type` is a regex** (`correction` if the text reads as disagreement, else `principle`) — harvested from bootstrap, unchanged. Comments written after the reveal may discuss the AI's number; unlike Zoom `tool_feedback` they are stored. Watch for "the AI scored this too high" chunks showing up in citations; if they do, filter them at ingest the way `zoom-2` does.
- **Approve these two first when Chris is back** (pending, `zoom:2026-09-18`): 42:23 (quality and accuracy → component parts → relative to an ideal, in relationships between fundamentals, phase to phase) and 49:35 (the two comparisons). They are the missing Evaluate calibration; until they are retrievable, outcome-level comparisons will keep scoring a point above Chris. The endpoint exists (`/api/chunks/approve`, actor must be chris); the UI is session 8.
- **The clerk says yes to new fields.** Every boolean added to the schema so far has needed a code guard within a day (how_stated, comparison made, delivery_to_peer, states_effect). Rule for any future field: define it by what it is NOT, and ship the deterministic check with it.
- **`evidenceGuards` equipment check is lexical** (modal verbs in every sentence that names equipment). It will miss a recommendation phrased in the present tense and could mis-fire on "her boots should have held her up and didn't". Logged in `extraction_guards` so a mis-fire is visible in the result.
- **Desired Performances with an ideal stated but `fundamentals_blended: []`** scored 3 on 7uk7lnh (Chris 2). The unit says "multiple fundamentals in blended relationship"; an outcome-only ideal arguably is a partial instance. Same family as the Evaluate depth question — do not tune before the third scorecard and Chris's chunks.

- **Journal sheet needs an `entryType` header (Mark, one cell).** Journal tab, row 1, first empty cell (column V): `entryType`. Until then no app can store an entry's type and session 7's done-when ("one entry of each type") cannot be met. Afterwards, set the type on the existing entries with Edit. The Apps Script's `autoCreateTab` header list has the same omission — add it there the next time that file is touched, so a rebuilt Sheet doesn't regress.
- **`_MENTOR_PROFILES.chris.email` is Mark's Gmail; Gates and Mike have no profile.** `notify` mails every profile with an email. Put Chris's real address in (old app → Profile tab, as Chris; or edit the Config cell) before counting "notification email fires" as done. The new-entry form prints exactly who will be mailed.
- **Journal mentor comments reach the knowledge store only through `bootstrap.mjs`.** §6.3 "continuous" is wired for MA History alone: `/api/ingest/comment` takes a `sessionId`. Backend follow-up (no new function): accept `{ journalId, timestamp }` and re-read the Journal row the same way. Until then, re-run bootstrap after Chris comments on entries.
- **Test entry `7a8up2m` ("sadfasdfa", 2026-09-20) is in the live Journal** — delete it from the new tab (the old app has no journal delete).
- **A mentor's comment or depth read does not notify Mark** (nor did the old app). If wanted: `notify` mails mentors only; a candidate address would need a profile entry and a `to` filter in the Apps Script.
- **Mentors see each other's depth reads before giving their own** — kept from the old app. Depth is coaching signal, not agreement data, so no seal; revisit if it ever feeds a metric.
- **Login now makes three parallel `getAll` calls** (MASessions, Config, Journal). Each retries on its own; if Apps Script transient failures get more frequent as tabs are added (sessions 8–10), load lazily per tab or serialize.
- **Config has two `vid_qwn6hc1` rows** (seen in the session 7 export) — a duplicate from the old update→create fall-through. Session 9 (Videos): dedupe on load, newest wins.
- **The notification links to the new app.** Chris is nominally on the old URL until session 8; both read the same Sheet, and the new Journal tab is complete for a mentor, so the link is safe to follow.
- **Step 1 is slow and getting slower: 179 s of model time for one extraction (2026-09-21), against ~90 s in session 5 and a 300 s function limit.** The cost is output size — the extraction JSON is ~22 KB. The heartbeat keeps the connection alive; it does not buy time. If a long exam approaches 300 s: (a) stream from Anthropic in `lib/llm.js` (`messages.stream`) so a stall is detected early instead of at the 240 s client timeout + one retry, which alone can exceed the limit; (b) trim the extraction schema (verbatim quotes are the bulk); (c) split Step 1 by exam section and run the parts in parallel. Needs a backend session and a before/after on both scorecards — not a hotfix.

